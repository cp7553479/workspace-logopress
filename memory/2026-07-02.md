# 2026-07-02 Memory

- During ASI ecommerce image batch continuation, user asked by voice why processing did not continue. We explained the interruption: after LP4595 and LP4544 were delivered, `.temp/asi-image-task/status.md` said to scan/dispatch the next batch, but `HEARTBEAT.md` did not retain a continue instruction, so scheduled wakeups saw no heartbeat task and did not dispatch new products. It was not an image-generation or Base writeback failure.
- Recovery actions taken: replied to user in Feishu, restored `HEARTBEAT.md` with continuation instructions, updated `.temp/asi-image-task/status.md`, and spawned the next two product subagents for ASI image processing: LP4553 and LP4561.
- Durable process rule reinforced: ASI ecommerce image batches for `商品表` should be orchestrated by the main session as a product queue; one subagent handles exactly one product; parallel subagents must process different products; main session owns Base writeback verification, user delivery, and maintaining `.temp/asi-image-task/status.md` plus `HEARTBEAT.md` for recovery.
# 2026-07-02 Memory

- During ASI ecommerce image batch continuation, user asked by voice why processing did not continue. We explained the interruption: after LP4595 and LP4544 were delivered, `.temp/asi-image-task/status.md` said to scan/dispatch the next batch, but `HEARTBEAT.md` did not retain a continue instruction, so scheduled wakeups saw no heartbeat task and did not dispatch new products. It was not an image-generation or Base writeback failure.
- Recovery actions taken: replied to user in Feishu, restored `HEARTBEAT.md` with continuation instructions, updated `.temp/asi-image-task/status.md`, and spawned the next two product subagents for ASI image processing: LP4553 and LP4561.
- Durable process rule reinforced: ASI ecommerce image batches for `商品表` should be orchestrated by the main session as a product queue; one subagent handles exactly one product; parallel subagents must process different products; main session owns Base writeback verification, user delivery, and maintaining `.temp/asi-image-task/status.md` plus `HEARTBEAT.md` for recovery.

- Later ASI ecommerce image batch progress: delivered and Base-verified LP5920, LP5921, LP5922, LP5937, and LP5928. Parent/main session performed final verification before user delivery; sent generated images through Feishu after confirming Base writeback counts. LP5920 skipped SKU chart because only reliable brown single-color evidence existed. LP5921 produced SKU chart covering 13 colors. LP5922 skipped extra per-color white-background variants because no reliable per-color source images existed. LP5937 skipped SKU chart because only reliable white single-color evidence existed; rejected detail draft was sent separately with reason. LP5928 sent accepted final images and separately noted rejected drafts where SKU chart merged Black/Brown and first hero showed only two variants.
- Active ASI image queue state near 2026-07-02 15:39 Asia/Shanghai: LP5957 was active in Codex lane and LP5966 was active in Oracle-primary lane. Previous active/retry products LP5928 and LP5937 completed. LP5957 completion handling had begun around the latest context, with follow-up expected to verify Base writeback, send images, record status, and dispatch the next eligible product.
- User added a durable operating requirement for the ASI image queue: every time any subagent completes, first restate the task, then continue by spawning the next subagent for the next product. Restatement should include: process 商品表 records where `Mark=有询单`, `参考图片` exists, and `商品主图` is empty; one subagent handles exactly one product; two parallel lanes must use different products; subagent generates images while main session verifies Base writeback, sends images, records status, and dispatches the next product.
- During ASI queue heartbeats, if subagent runs are cancelled by runtime compaction or heartbeat interruption, do not assume task failure. Inspect `.temp/asi-image-task/status.md`, local output directories, Base record fields, and active OpenClaw sessions; if local outputs are still appearing recently, classify as still progressing and avoid duplicate dispatch for the same product. If a lane is genuinely empty after parent verification, select the next live Base eligible product and dispatch it with the full task restatement embedded in the subagent requirement.
