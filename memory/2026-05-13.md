
- 2026-05-13: 用户提示 ACP 后续用 `mode: run` 时加 `thread: true` 进行（若当前工具 schema 支持）。

- 2026-05-13: 用户更正 ACP 启动偏好：应使用 `runtime: "acp"`, `agentId: "codex"`, `mode: "session"`, `thread: true`（若工具/版本支持）。

- 2026-05-13: 用户指出我在子任务/上传完成后没有主动及时汇报。以后长任务完成并验收后必须主动发最终结果；不要等用户追问。收到 subagent 完成通知后立即接续：查产物→验图→上传/验表→主动汇报。
