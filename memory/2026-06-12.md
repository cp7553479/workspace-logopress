# Short-Term Memory - 2026-06-12

## 2026-06-12 12:49 CST - New ASI has-inquiry missing-main queue

- Trigger: Chen Peng asked to filter `商品表` for records where `Mark` is `有询单` and `商品主图` has no image files, then generate ASI商品套图 for each product.
- Verified scope: Base `SF9ibzjI4a0YFJsDZtKcLvtbnfh`, table `tbl3KNB7zP8EQ1d0` (`商品表`). Server-side filter used `Mark intersects ["有询单"]` and `商品主图 isEmpty`. Pagination showed 656 matching records: offsets 0/200/400 returned 200 each, offset 600 returned 56, offset 800 returned 0.
- First batch selected: LP1277 (`recuYsAdUpxzmv`) and LP1356 (`recuYsAdUp4j8c`). Both have one `参考图片`, empty `白底图`, and empty `商品主图`.
- First batch subagents spawned at 2026-06-12 12:52 CST:
  - LP1277: agent `019eba2d-c219-7291-8bb2-9905729163a2` (Bohr)
  - LP1356: agent `019eba2d-c660-7621-9ce0-8c41e99b711f` (Godel)
- 2026-06-12 12:59 CST correction after resuming: `openclaw.subagents` showed no active LP1277/LP1356 workers and `.temp/asi_has_inquiry_manifest/progress.json` showed `running_count=0`. Treat the earlier 12:52 "spawned" note as unverified/stale unless later session history proves otherwise. Prepared fresh handoffs `handoff_LP1277_20260612_1300.md` and `handoff_LP1356_20260612_1300.md` for a new two-worker batch.
- 2026-06-12 13:00 CST actual two-worker batch spawned:
  - LP1277 `recuYsAdUpxzmv`: session `agent:global-trading-product:subagent:7844b231-c091-4e6b-af1b-fb7633f1510d`, run `10ad75a5-fa49-4fe2-a3b9-9dedd72e2c1a`.
  - LP1356 `recuYsAdUp4j8c`: session `agent:global-trading-product:subagent:7f8fc6fa-7c10-4785-845a-d2f4c0db7fa6`, run `39fa8069-3d11-48d7-b8f6-01557b844d6a`.
  Current batch state is also recorded in `.temp/asi_has_inquiry_manifest/current_batch_20260612_1300.md`. Do not spawn a third product until one of these completes, blocks, or clearly exits.
- User-specific operating rule for this run: at most 2 product-image subagents active at the same time; each subagent handles exactly one product. New products require new subagents.
- Required pre-step per product: use the original `参考图片` only to generate a clean no-logo reference image; remove product logo artwork/logo patterns, brand marks, sample logos, imprints, manufacturer/platform marks, QR/watermark/contact/URL while preserving the exact real product. If the reference contains the plain text `YOUR LOGO`, keep that text. Upload the clean image back to `参考图片` and replace the old file, not append alongside it; read back to verify the replacement before any ASI套图 generation.
- Image-generation route for this run: alternate Oracle ChatGPT and Oracle Gemini. Use Codex only when Oracle errors; before using Codex for the next image, test Oracle recovery first, and use Codex only if Oracle is still unavailable. Record the actual path and failure evidence for every image.
- Supervisor obligations: update `update_plan` while working, write progress/status here, keep `HEARTBEAT.md` with checks for subagent progress, and nudge long-unresponsive subagents.
- 2026-06-12 13:08 CST clarification: `HEARTBEAT.md` should be a compact event reminder for what to inspect, not a duplicate task history. Keep detailed product rules, worker handoffs, and status history in memory/progress/handoff files; keep heartbeat prompts short and pointer-based.
- 2026-06-12 13:15 CST correction: do not hard-code current product numbers such as LP1277/LP1356 into heartbeat prompts for a queue task. Heartbeat should check the current active/running ASI image subagents and queue state from progress files, then spawn/deliver/nudge according to capacity and rules.
- 2026-06-12 13:21 CST heartbeat: checked current ASI image queue. Subagent list from main scope showed no active workers, but product temp folders and Oracle processes showed real progress for the current two-worker batch. Updated `.temp/asi_has_inquiry_manifest/progress.json` from stale `running_count=0` to `running_count=2`. LP1277 had fresh outputs through material detail; LP1356 had clean reference, white background, lifestyle, and selling-point outputs with a ChatGPT selling-point retry process active. Did not spawn another product because both slots were occupied. Did not deliver images yet because workers had not produced final main-image upload/readback reports.
- 2026-06-12 13:32 CST prompt rule: for ASI product image generation, never include `产品编号` such as LP1277/LP1356, record_id, or other internal tracking IDs in image-generation prompts or image-visible text. These IDs are only for folders, filenames, upload logs, handoffs, and progress tracking. Created Skill Workshop update proposal `asi-product-image-generator-20260612-69b747ea26`.
- 2026-06-12 13:37 CST: LP1277 (`recuYsAdUpxzmv`) completed. Parent verified Base readback: `参考图片=1` (`LP1277_clean_no_logo.png`), `白底图=1` (`LP1277_white_background.png`), and `商品主图=6` (`LP1277_hero_main_final.png`, `LP1277_lifestyle_wedding.png`, `LP1277_selling_point.png`, `LP1277_dimensions.png`, `LP1277_material_detail.png`, `LP1277_sku_chart.png`). All 8 generated images were sent to Feishu. Oracle ChatGPT/Gemini paths succeeded; Codex fallback was not used. Invalid hero attempt containing filename text was rejected and not uploaded.
- 2026-06-12 13:40 CST: After LP1277 opened one worker slot, LP1358 (`recuYsAdUpgXFU`) was verified as eligible (`Mark=有询单`, `参考图片=1`, `白底图` empty, `商品主图` empty), given handoff `.temp/asi_has_inquiry_manifest/handoff_LP1358_20260612_1340.md`, and spawned as subagent `agent:global-trading-product:subagent:d49359d8-f8f5-4a4c-b9f8-4bd7319d66fb` / run `781dc684-0caf-4b0c-86d7-206e0d47c6c3`. Handoff explicitly forbids product number, record_id, or internal IDs in image-generation prompts.
- 2026-06-12 13:52 CST user correction: for future ASI套图 generation, spawn only one product-image subagent at a time and wait for that worker to complete before spawning the next. LP1358 and LP1362 were already running before the correction; do not spawn any replacement while either remains active. Updated `HEARTBEAT.md`, current batch notes, and shared worker instructions accordingly.
- 2026-06-12 13:55 CST heartbeat: checked ASI image queue under the new serial rule. LP1358 and LP1362 were both already active and showed fresh local progress within minutes, so no nudge and no new spawn. LP1358 had outputs through white background, lifestyle, selling point, and dimension chart but no final upload/readback report. LP1362 had clean-reference generation/upload and original-reference removal activity. Updated `.temp/asi_has_inquiry_manifest/progress.json`; no user notification needed.
- 2026-06-12 14:21 CST: LP1358 (`recuYsAdUpgXFU`) completed parent review and delivery. Parent verified local PNGs, prompt files had no product number/record_id/internal ID, and live Base readback showed `参考图片=1`, `白底图=1`, `商品主图=7`. Sent clean reference, white background, hero, lifestyle, selling point, dimension, material/detail, and two SKU sheets to Feishu. Updated progress/current batch; LP1362 remains the only active worker, so no new product was spawned under the serial rule.

## ASI product image batch operations
- 2026-06-12 00:56 CST: LP1050 (`recuYsAdUpKzAe`) completed, uploaded, Base-readback verified, and delivered to the user. Final Base state: `白底图=1` (`LP1050_white_multicolor.png`, token `DOntbL50QofTLBxNym5csWlpnhg`) and `商品主图=6` (`LP1050_lifestyle.png`, `LP1050_selling_point.png`, `LP1050_hero.png`, `LP1050_dimension.png`, `LP1050_material_detail.png`, `LP1050_sku_chart.png`) with no duplicate main names/tokens. All 7 final images were sent in Feishu with media receipts. Progress manifest was updated to `completed_count=130`, `running_count=0`, `retry_queue_count=0`, `blocked_count=0`.
- LP1050 generation path evidence: hero via Oracle ChatGPT browser path; dimension via Oracle Gemini browser path; material detail via Oracle Gemini strict regeneration after a ChatGPT upload timeout; SKU via Oracle ChatGPT browser path; white/lifestyle/selling point reused valid existing outputs. This satisfied the user requirement that new generated images be sent to chat, not only uploaded to Base.

## Scheduling preference
- User requested replacing HEARTBEAT monitoring with a cron job pinned to the current Feishu private session. `HEARTBEAT.md` was cleared to zero bytes. ASI monitoring cron job `asi-subagent-watch-current-session` was created/enabled with job id `01cc878d-2381-4d6d-b439-77d90c6ad2f5`, interval every 30 minutes, `sessionTarget=session:agent:global-trading-product:feishu:global-trading-product:direct:ou_61123e30e1c1bfd2d0d31af923d4982d`, delivery announce to the same Feishu DM.
- Cron payload requirements: check `.temp/asi_has_inquiry_manifest/progress.json`, active/recent subagents, local Oracle/image processes, current product outputs, Base readback, and delivery state; send newly generated images directly to the user; keep only one product-image subagent running; report progress/blockers/recovery/delivery in the current Feishu private chat.

## Updated ASI product image rules
- User added a mandatory pre-step for all future product-image subagent handoffs: before generating any white-background or ASI image set, use the product's original `参考图片` field image only to generate a clean no-logo product reference image. Remove existing product logo artwork, sample logos, brand marks, imprints, manufacturer/platform marks, QR codes, watermarks, contact info, and URLs while preserving the exact real product structure, color, material, proportions, accessories, and SKU details. Subsequent white-background images and ASI套图 must be generated from this clean no-logo reference, not directly from the original `参考图片`.
- User then upgraded the image-path rotation rule: future product-image subagent handoffs must require three-way alternation among Oracle ChatGPT, Oracle Gemini, and Codex image generation. Do not rely on only one path. Record the actual path used for every generated image; if a path fails due quota, login, model availability, browser/page issue, network, auth, or reference-image support, record exact evidence before switching to another available path.
- These rules were written into `skills/asi-product-image-generator/SKILL.md`, `.temp/asi_has_inquiry_manifest/worker_instructions.md`, the `asi-subagent-watch-current-session` cron payload, and long-term memory. `HEARTBEAT.md` remained zero bytes after the changes.

## ASI reference-image replacement rule
- 2026-06-12 01:16 CST: User clarified the clean no-logo reference image must replace the original image in the product record's `参考图片` field, not only be created locally and used as an internal reference. Future product-image subagent handoffs must require: use the original `参考图片` attachment only to generate a clean no-logo product reference; upload that clean image back to the current record's `参考图片` field; remove/replace the original reference image; read back the field to confirm the original is gone and only the clean no-logo reference remains; then generate white-background and ASI套图 from that clean field image. Do not directly use the original `参考图片` image for ASI套图.
- Updated `skills/asi-product-image-generator/SKILL.md`, `.temp/asi_has_inquiry_manifest/worker_instructions.md`, `MEMORY.md`, and cron job `asi-subagent-watch-current-session` (`01cc878d-2381-4d6d-b439-77d90c6ad2f5`) with this replacement rule. Verified `HEARTBEAT.md` is still 0 bytes.

## 2026-06-12 11:27 CST - Pre-compaction flush

- ASI 商品图生图路径偏好更正：用户先误说下次 subagent 要用 Codex 生图，随后立即更正为“请在下次派生 subagent 的时候一定要使用 Oracle 生图”。后续商品/电商生图 subagent handoff 必须明确要求使用 Oracle 生图；不要默认改用 Codex。若 Oracle 因额度、登录、页面异常、网络、认证等问题失败，需记录失败证据和恢复点。
- 已在本轮工作中把 ASI 监控 cron 的 handoff 文本改回“必须 Oracle 生图”，并创建了 `asi-product-image-generator` skill 更新提案；工作区 `MEMORY.md` 也已在之前步骤更新，但本次 memory flush 不再编辑该文件。
- ASI 监控任务状态：LP2575 已完成并交付，progress 当时显示 running=0、retry_queue=0、blocked=0，last_completed=LP2575，且 verified / sent_to_user；Base 回读已有 clean reference、白底图和 5 张主图，本地合计 6 张交付图。HEARTBEAT.md 当时已清空。
- 最近聊天中还出现了 LP1143 与 LP1083 的图片交付消息：LP1143 至少发送了 hero_main、lifestyle、selling_point、material_detail；LP1083 至少发送了 clean_no_logo_reference 以及多个 white/black/green/grey 相关白底或变体图。由于上下文被截断，后续恢复时应先查 progress.json、Base 回读、发图回执和本地输出再确认它们是否完整完成。
- 长任务恢复要点：继续遵守商品图 subagent 串行规则，主会话一次只派生一个商品图 subagent；新生成图片必须直接逐张发给用户，不能只上传 Base；派发前仍要求先生成并回填干净无 logo 参考图，再基于该参考图做白底图和 ASI 套图。
## ASI image queue update - 13:46

Context: continued the `Mark=有询单` and empty `商品主图` ASI image queue with a maximum of two active product workers.

Actions and outcome:
- Verified LP1356 (`recuYsAdUp4j8c`) worker completion from local outputs and Base readback: `参考图片=1`, `白底图=1`, `商品主图=6`.
- Sent LP1356 clean reference, white background, and six main images to the user in Feishu.
- Confirmed LP1356 prompt files did not contain product number or record ID.
- Updated shared worker instructions so future image-generation prompts must not include product numbers, record IDs, or internal tracking IDs.
- Spawned LP1362 (`recuYsAdUpeJd4`) after LP1356 completion opened a worker slot. Current active workers are LP1358 and LP1362.

Lesson / next rule: product identifiers are internal tracking only. They may appear in folders, filenames, logs, handoffs, and progress, but not in image-generation prompts or visible image text.

## ASI image queue update - 14:29

Context: continued the serial ASI product-image queue after LP1362 completion.

Actions and outcome:
- Verified LP1362 (`recuYsAdUpeJd4`) worker completion from local PNGs, prompt scan, and Base readback: `参考图片=1`, `白底图=1`, `商品主图=6`.
- Sent LP1362 clean reference, white background, and six main images to the user in Feishu.
- Recorded LP1362 as `completed_uploaded_verified_delivered` in `.temp/asi_has_inquiry_manifest/progress.json`.
- Rechecked the next eligible candidate before dispatch: LP1373 (`recuYsAdUpel0L`) still has `Mark=有询单`, one `参考图片`, empty `白底图`, and empty `商品主图`.
- Spawned exactly one new worker for LP1373 under the serial rule: session `agent:global-trading-product:subagent:955d0bf8-fc02-46e2-889f-df430201ca8c`, run `d26e2916-118f-4c3e-9a13-3ff604187021`, handoff `.temp/asi_has_inquiry_manifest/handoff_LP1373_20260612_1428.md`.

Lesson / next rule: after each ASI worker completes, the parent must verify/upload-delivery state, mark it complete, then re-read the next candidate's live record before spawning one new worker.

- 2026-06-12 14:38 CST progress check: User asked to inspect task status and continue proactively. LP1373 is the only active ASI product-image worker under the serial rule. Local evidence shows clean no-logo reference and white-background outputs plus upload/readback records exist; prompt scan found no product number/record_id/internal ID terms; Oracle ChatGPT lifestyle generation is currently running with `.temp/asi_oracle_image.lock`. No new worker spawned while LP1373 is active.

- 2026-06-12 14:51 CST heartbeat: LP1373 remains the only active ASI product-image worker under the serial rule. Clean reference and white-background upload/readback records exist; generated outputs now include lifestyle, selling point, dimension, and material detail. Oracle ChatGPT SKU color chart generation is running with `.temp/asi_oracle_image.lock`. No final main-image upload/readback yet, so no delivery and no new worker spawned.

## ASI image queue update - 15:18

Context: continued the serial ASI product-image queue after LP1373 completion.

Actions and outcome:
- Verified LP1373 (`recuYsAdUpel0L`) worker completion from local PNGs, prompt scan, live Base readback, and cleared Oracle lock: `参考图片=1`, `白底图=1`, `商品主图=6`.
- Sent LP1373 clean reference, white background, and six main images to the user in Feishu.
- Recorded LP1373 as `completed_uploaded_verified_delivered` in `.temp/asi_has_inquiry_manifest/progress.json`.
- Rechecked the next eligible candidate before dispatch: LP1378 (`recuYsAdUpL6tW`) still has `Mark=有询单`, one `参考图片`, empty `白底图`, and empty `商品主图`.
- Spawned exactly one new worker for LP1378 under the serial rule: session `agent:global-trading-product:subagent:e2e77099-1170-47a0-80d9-3b4438267b9c`, run `d9c3c950-0c4d-4206-93ba-896e8dd725f8`, handoff `.temp/asi_has_inquiry_manifest/handoff_LP1378_20260612_1517.md`.

Lesson / next rule: duplicate subagent completion events can arrive; parent should not repeat delivery if already handled. Verify once, deliver once, update progress/current batch, then spawn only one next worker after running is clear.

- 2026-06-12 15:24 CST heartbeat: LP1378 remains the only active ASI product-image worker under the serial rule. Clean no-logo reference exists and `参考图片` readback confirms exactly one clean reference. Prompt scan on prompt files found no product number/record_id/internal ID terms. Oracle Gemini white-background generation is running with `.temp/asi_oracle_image.lock`. No final main-image upload/readback yet, so no delivery and no new worker spawned.

## ASI image queue update - 15:30

Context: continued the serial ASI product-image queue after LP1378 completion.

Actions and outcome:
- Verified LP1378 (`recuYsAdUpL6tW`) worker completion from local PNGs, prompt scan, live Base readback, and cleared Oracle lock: `参考图片=1`, `白底图=1`, `商品主图=6`.
- Sent LP1378 clean reference, white background, and six main images to the user in Feishu.
- Recorded LP1378 as `completed_uploaded_verified_delivered` in `.temp/asi_has_inquiry_manifest/progress.json`.
- Rechecked the next eligible candidate before dispatch: LP1382 (`recuYsAdUpTdwo`) still has `Mark=有询单`, one `参考图片`, empty `白底图`, and empty `商品主图`.
- Spawned exactly one new worker for LP1382 under the serial rule: session `agent:global-trading-product:subagent:d93e92ba-eb32-4956-a323-ebb195de15e7`, run `b7e61d46-1088-4907-b876-b8c9aaea7e35`, handoff `.temp/asi_has_inquiry_manifest/handoff_LP1382_20260612_1530.md`.

Lesson / next rule: continue using live record checks before each serial spawn; do not trust stale candidate files alone.

## ASI image queue update - 16:06

Context: continued the serial ASI product-image queue after LP1382 completion.

Actions and outcome:
- Verified LP1382 (`recuYsAdUpTdwo`) worker completion from local PNGs, prompt scan, live Base readback, and cleared Oracle lock: `参考图片=1`, `白底图=1`, `商品主图=6`.
- Sent LP1382 clean reference, white background, and six main images to the user in Feishu.
- Recorded LP1382 as `completed_uploaded_verified_delivered` in `.temp/asi_has_inquiry_manifest/progress.json`.
- Rechecked the next eligible candidate before dispatch: LP1388 (`recuYsAdUpKqmt`) still has `Mark=有询单`, one `参考图片`, empty `白底图`, and empty `商品主图`.
- Spawned exactly one new worker for LP1388 under the serial rule: session `agent:global-trading-product:subagent:a7eb0260-8da9-4694-a618-00e07e09a7d4`, run `668a639c-9188-4d7a-abc5-515abc27bda2`, handoff `.temp/asi_has_inquiry_manifest/handoff_LP1388_20260612_1606.md`.

Lesson / next rule: keep delivering generated images before marking a worker completed; only after the parent sends all final images and running is clear should the next worker be spawned.

- 2026-06-12 16:26 CST heartbeat: LP1388 is the only active ASI product-image worker under the serial rule, but parent found no local product temp folder, no outputs/uploads, no Oracle lock, and no active LP1388 generation process about 20 minutes after spawn. Parent sent a targeted nudge to the LP1388 subagent asking it to start from the handoff or report exact blocker. No delivery and no new worker spawned.

## ASI image queue update - 16:31

Context: continued the serial ASI product-image queue after LP1388 completion.

Actions and outcome:
- Verified LP1388 (`recuYsAdUpKqmt`) worker completion from local PNGs, prompt scan, live Base readback, and cleared Oracle lock: `参考图片=1`, `白底图=1`, `商品主图=6`.
- Sent LP1388 clean reference, white background, and six main images to the user in Feishu.
- Recorded LP1388 as `completed_uploaded_verified_delivered` in `.temp/asi_has_inquiry_manifest/progress.json`.
- Rechecked the next eligible candidate before dispatch: LP1396 (`recuYsAdUpOyB5`) still has `Mark=有询单`, one `参考图片`, empty `白底图`, and empty `商品主图`.
- Spawned exactly one new worker for LP1396 under the serial rule: session `agent:global-trading-product:subagent:f94c44e9-8330-4326-8ea2-f4e7d4d892b9`, run `1c641e60-dae3-4a59-bacd-765e5c24cc99`, handoff `.temp/asi_has_inquiry_manifest/handoff_LP1396_20260612_1631.md`.

Lesson / next rule: if a worker appears stalled but then completes after a nudge, still perform full parent verification and delivery before clearing running and dispatching the next single worker.

- 2026-06-12 17:12 CST LP1396 recovery: the first LP1396 worker timed out after completing clean reference replacement and white-background upload/readback. Parent verified local outputs and live Base state: `参考图片=1` (`clean_reference_oracle_chatgpt.png`), `白底图=1` (`white_background_oracle_gemini.png`), `商品主图=0`; no Oracle lock or active LP1396 generation process remained. Parent marked LP1396 as needing recovery, wrote `.temp/asi_has_inquiry_manifest/handoff_LP1396_recovery_20260612_1711.md`, and spawned recovery worker `agent:global-trading-product:subagent:e65cffdc-7ddb-412f-81eb-9939279b0c75` / run `c1853dd5-81c1-4543-b766-16e311264ac7` for the same product only, limited to missing main images. Serial queue remains on LP1396; do not spawn the next product until LP1396 is completed, verified, delivered, and cleared from running.

- 2026-06-12 17:18 CST LP1396 status check: recovery worker is active on the missing main-image stage. Oracle ChatGPT lifestyle generation started at `2026-06-12T17:14:46+08:00` with `.temp/asi_oracle_image.lock` present as a directory; only `clean_reference_oracle_chatgpt.png` and `white_background_oracle_gemini.png` exist so far. Prompt scan over LP1396 prompts/outputs found no product number, record ID, base token, table ID, or internal ID strings. No delivery and no next product spawn; serial queue remains on LP1396.

- 2026-06-12 17:21 CST heartbeat: LP1396 recovery subagent `agent:global-trading-product:subagent:e65cffdc-7ddb-412f-81eb-9939279b0c75` is running. Oracle ChatGPT lifestyle generation has been running about 7 minutes with `.temp/asi_oracle_image.lock` present and log in normal waiting-for-image-response state. No new main-image output yet, so no upload/readback/delivery and no next worker spawn.

## ASI image queue update - 17:55

Context: continued the serial ASI product-image queue after LP1396 recovery completion.

Actions and outcome:
- Verified LP1396 (`recuYsAdUpOyB5`) from local PNGs, prompt scan, live Base readback, cleared Oracle lock, and recovery child completion: `参考图片=1`, `白底图=1`, `商品主图=6`.
- Sent LP1396 clean reference, white background, and six main images to the user in Feishu.
- Recorded LP1396 as `completed_uploaded_verified_delivered` in `.temp/asi_has_inquiry_manifest/progress.json`.
- Original LP1396 worker timed out after clean/white; recovery generated only missing main images. Accepted main images: hero_main_oracle_chatgpt, lifestyle_scene_oracle_chatgpt_plain, selling_point_oracle_chatgpt, dimension_chart_oracle_gemini, material_detail_oracle_chatgpt, sku_chart_oracle_gemini.

Lesson / next rule: parent may accept a recovery worker only after final Base readback and user delivery; then re-check for no active subagents before spawning the next single worker.

## ASI image queue update - 17:58

Context: continued the serial ASI product-image queue after LP1396 completion and delivery.

Actions and outcome:
- Confirmed no active ASI subagents after LP1396 recovery finished.
- Skipped stale `next_batch` candidates whose live Base records already had main images: LP2226, LP6063, LP1214, LP1004, LP1016, LP1024, LP1027, LP1030, LP1032, LP1050, LP1055, LP1083, LP1117, LP1133, LP1143, LP1269, LP2575, LP1277, LP1356, LP1358, LP1362, LP1373, LP1378, LP1382, and LP1388.
- Rechecked the next live eligible candidate after LP1396: LP1461 (`recuYsAdUpZtrj`) with `Mark=有询单`, one `参考图片`, empty `白底图`, and empty `商品主图`.
- Spawned exactly one new worker for LP1461 under the serial rule: session `agent:global-trading-product:subagent:994b52c7-e2df-4d23-8be7-4f5e1761f2d1`, run `521ec73d-3a0b-4f70-a330-f303d8d2cb9d`, handoff `.temp/asi_has_inquiry_manifest/handoff_LP1461_20260612_1758.md`.

Lesson / next rule: stale batch pointers must be treated only as hints; always live-check candidates and skip records that already have main images before spawning the next single worker.

## ASI subagent dispatch rule update - 18:02

Trigger: user reminded that future product-image subagent dispatches should prioritize Oracle, alternate ChatGPT and Gemini, and only switch to Codex after both Oracle paths fail.

Action:
- Updated `.temp/asi_has_inquiry_manifest/worker_instructions.md`.
- Updated current LP1461 handoff `.temp/asi_has_inquiry_manifest/handoff_LP1461_20260612_1758.md`.
- Updated `MEMORY.md` stable preference.
- Sent a follow-up instruction to the active LP1461 worker to re-read the handoff and worker instructions.

Lesson / next rule: every future product-image subagent handoff should clearly describe the task or point to a product-specific handoff document, require reading that document first, prefer Oracle generation, alternate Oracle ChatGPT/Gemini, and use Codex only after both Oracle routes fail or are unavailable.

- 2026-06-12 18:33 CST LP1461 status check: parent checked queue after user request. LP1461 is still the only active product-image task. Local outputs include clean no-logo reference, white background, and main-image candidates for lifestyle, selling point, dimension, material detail, and SKU; hero main is currently generating via Oracle with `.temp/asi_oracle_image.lock`. Base live readback confirms `参考图片=1`, `白底图=1`, `商品主图=0`. Prompt/internal-ID scan over LP1461 outputs found no product number, record ID, base token, table ID, or internal ID strings. Dimension fallback to Codex has Oracle failure evidence from Gemini quota/reset and ChatGPT fetch failure logs. No delivery and no next product spawn until hero/final upload/readback completes.

## ASI image queue heartbeat - 18:57

Context: heartbeat checked the serial ASI product-image queue after LP1508 subagent timeout.

Actions and outcome:
- LP1508 original worker timed out after clean reference replacement only.
- Parent verified no active subagents and no active Oracle generation process.
- Local clean reference exists at `temp/LP1508_asi_recuYsAdUpH2uo/outputs/LP1508_clean_no_logo_oracle_chatgpt.png`.
- Live Base readback for LP1508 confirmed `参考图片=1`, `白底图=0`, `商品主图=0`.
- Prompt scan over LP1508 prompt files found no product number, record ID, base token, table ID, or internal ID terms.
- A stale zero-byte `.temp/asi_oracle_image.lock` file had no corresponding process and was archived under `.temp/stale_locks/`.
- Parent wrote `.temp/asi_has_inquiry_manifest/handoff_LP1508_recovery_20260612_1856.md` and spawned one same-product recovery worker for missing white-background and main images only.

Lesson / next rule: if a worker times out after a partial Base write, verify live Base field counts and recover the same product before considering any next queue item.

## ASI image queue update - 18:44

Context: continued the serial ASI product-image queue after LP1461 completion.

Actions and outcome:
- Verified LP1461 (`recuYsAdUpZtrj`) from local PNGs, prompt scan, live Base readback, cleared Oracle lock, and child completion: `参考图片=1`, `白底图=1`, `商品主图=6`.
- Sent LP1461 clean reference, white background, and six main images to the user in Feishu.
- Recorded LP1461 as `completed_uploaded_verified_delivered` in `.temp/asi_has_inquiry_manifest/progress.json`.
- Confirmed no active subagents after LP1461.
- Rechecked the next live eligible candidate: LP1508 (`recuYsAdUpH2uo`) with `Mark=有询单`, one `参考图片`, empty `白底图`, and empty `商品主图`.
- Spawned exactly one new worker for LP1508 under the serial rule: session `agent:global-trading-product:subagent:33fffd8a-c315-4166-bb5b-751fd8dacfdd`, run `d49ab729-b91f-4582-b3d5-15bc5e6e21f2`, handoff `.temp/asi_has_inquiry_manifest/handoff_LP1508_20260612_1844.md`.

Lesson / next rule: future product-image subagent handoffs must include a clear task document, prioritize Oracle, alternate Oracle ChatGPT/Gemini where possible, and use Codex only after both Oracle paths fail or are unavailable for that image.

## ASI image queue update - 19:33

Context: LP1508 recovery subagent completed and parent reviewed the result.

Actions and outcome:
- Parent verified LP1508 local PNG outputs are 1254 x 1254 and visually acceptable.
- Prompt scan over LP1508 output/prompt files found no product number, record ID, base token, table ID, file token, or internal ID strings.
- Live Base readback for LP1508 confirmed `参考图片=1`, `白底图=1`, `商品主图=5`.
- Parent sent LP1508 clean reference, white background, and five main images to the user; every image send returned a media receipt.
- SKU/color chart was skipped because only one silvery variant is supported.
- Progress was updated with LP1508 as `completed_uploaded_verified_delivered` and running cleared.
- Next stale candidate LP2021 was skipped because live Base already had `白底图=2` and `商品主图=6`.
- Live Base check found LP1518 (`recuYsAdUpnDu7`) eligible: Mark `有询单`, `参考图片=1`, `白底图=0`, `商品主图=0`.
- Parent wrote `.temp/asi_has_inquiry_manifest/handoff_LP1518_20260612_1933.md` and spawned exactly one LP1518 worker under the serial rule.

Lesson / next rule: after a recovery worker completes, accept it only after parent verifies local outputs, prompt hygiene, live Base counts, image delivery receipts, progress update, and no active worker before spawning the next live-checked product.

## ASI image queue heartbeat - 19:47

Context: heartbeat checked LP1518 after the serial worker timed out.

Actions and outcome:
- LP1518 subagent status is `timeout`, but its Oracle browser generation process is still actively running for `lifestyle_scene_chatgpt.png`.
- Local outputs already include clean no-logo reference and white background.
- Live Base readback confirms `参考图片=1`, `白底图=1`, `商品主图=0`.
- Oracle lock is held by the active generation shell, not treated as stale.
- Parent updated `.temp/asi_has_inquiry_manifest/progress.json` and did not spawn a recovery worker or the next product while Oracle is still active.

Lesson / next rule: if a subagent times out but its delegated Oracle process is still running, treat the current product as active until that process finishes or clearly stalls; do not dispatch the next product or a recovery worker.

## ASI image queue heartbeat - 20:22

Context: heartbeat rechecked LP1518 after the earlier timeout and active Oracle generation.

Actions and outcome:
- LP1518 original worker remained timed out, and no active subagents were listed.
- Oracle lifestyle generation had finished, released the lock, and generated two local lifestyle candidates.
- Local clean reference, white background, and lifestyle candidates were verified as 1254 x 1254 PNGs.
- Live Base readback for LP1518 confirmed `参考图片=1`, `白底图=1`, `商品主图=0`.
- Prompt scan over LP1518 prompt/output paths found no product number, record ID, base token, table ID, file token, or internal ID terms.
- Parent wrote `.temp/asi_has_inquiry_manifest/handoff_LP1518_recovery_20260612_2021.md` and spawned exactly one same-product recovery worker for missing main images only: session `agent:global-trading-product:subagent:a86b847b-bfcf-4f73-adeb-b003c32404dd`, run `07d14c50-017b-4f5f-b2f2-d0378940c925`.

Lesson / next rule: after an Oracle process outlives a timed-out worker, wait for that process to finish, then recover the same product from live Base state before considering any next product.
## ASI image queue update - 20:39

Context: LP1518 recovery subagent completed and parent reviewed the result.

Actions and outcome:
- Parent verified LP1518 (`recuYsAdUpnDu7`) local outputs and final live Base readback: `参考图片=1`, `白底图=1`, `商品主图=6`.
- Parent sent the clean reference, white background, and six main images to the user; all sends returned media receipts.
- Accepted main images: `hero_main_chatgpt_LOGO-BK.png`, `lifestyle_scene_chatgpt.2.png`, `selling_point_gemini.png`, `dimension_chart_chatgpt.png`, `material_detail_gemini.png`, and `sku_color_chart_gemini.png`.
- Generation routes followed the Oracle-first rule: lifestyle/dimension/hero via Oracle ChatGPT; selling/material/SKU via Oracle Gemini. ChatGPT SKU fetch failed and Gemini SKU was accepted.
- Rejected `lifestyle_scene_chatgpt.png` because the second lifestyle candidate was cleaner. SKU chart omitted unsupported yellow/purple variants because reliable source support covered light blue, blue, green, black, orange, pink, and red.
- Progress was updated with LP1518 as `completed_uploaded_verified_delivered`; running queue was cleared.

Lesson / next rule: after a recovery worker finishes, parent must still verify Base readback and delivery receipts before clearing running and checking the next live candidate.

## ASI image queue update - 23:39

Context: LP1558 subagent completed and parent reviewed the result under the corrected heartbeat/spawn verification rules.

Actions and outcome:
- Parent verified LP1558 (`recuYsAdUp8EAB`) from local PNGs, Oracle lock/process check, prompt/internal-ID scan, subagent completion, saved readback, and a fresh live Base readback.
- Final Base counts: `参考图片=1`, `白底图=1`, `商品主图=6`.
- Parent sent clean reference, white background, and six main images to the user; all sends returned media receipts.
- Accepted main images: `hero_main_chatgpt_current_LOGO-BK.png`, `lifestyle_scene_chatgpt.png`, `selling_point_gemini.png`, `dimension_chart_chatgpt.png`, `material_detail_gemini.png`, and `sku_color_chart_chatgpt.png`.
- Rejected candidates: Gemini white background with multiple variants; Gemini lifestyle empty response; Gemini hero collage/small product; first ChatGPT hero failed before generation due model-picker mismatch.
- Progress was updated with LP1558 as `completed_uploaded_verified_delivered` and running cleared.
- Parent live-checked LP1559 (`recuYsAdUpWp54`) as next eligible: Mark `有询单`, `参考图片=1`, `白底图=0`, `商品主图=0`.
- Parent wrote `.temp/asi_has_inquiry_manifest/handoff_LP1559_20260612_2339.md`, confirmed no active subagents and no Oracle lock, called `sessions_spawn`, received child session `agent:global-trading-product:subagent:368f81f4-e859-4362-ad3c-fdabed02faa8` and run `634923f0-6a1c-4e84-9cb6-fdb91ca01a72`, then wrote `progress.json` with `running_count=1`.

Lesson / next rule: saying "dispatching" is not enough; dispatch is only real after `sessions_spawn` returns session/run IDs and `progress.json` verifies `running_count=1`.

## ASI image queue heartbeat - 20:51

Context: heartbeat continued the serial ASI product-image queue after LP1518 delivery.

Actions and outcome:
- Progress showed no running worker and LP1518 was already recorded as `completed_uploaded_verified_delivered`.
- Confirmed no active subagents and no Oracle lock.
- Live-checked the next candidate LP1538 (`recuYsAdUpt7DB`): Mark `有询单`, `参考图片=1`, `白底图=0`, `商品主图=0`.
- Reused the prepared product-specific handoff `.temp/asi_has_inquiry_manifest/handoff_LP1538_20260612_2040.md`.
- Spawned exactly one LP1538 worker under the serial rule: session `agent:global-trading-product:subagent:9de34500-b94d-496a-a4ed-5ce8e9b16a78`, run `5f0bcd6c-a6dd-4b03-9356-6739e788d69f`.

Lesson / next rule: if a handoff was prepared before a heartbeat boundary, still re-confirm no active worker and no Oracle lock before spawning it.

## ASI image queue heartbeat - 22:51

Context: heartbeat continued the serial ASI product-image queue after LP1538 completion.

Actions and outcome:
- Progress showed LP1538 had been completed, verified, delivered, and cleared from running.
- Confirmed no active ASI subagents and no Oracle lock.
- Live-checked LP1558 (`recuYsAdUp8EAB`): Mark `有询单`, `参考图片=1`, `白底图=0`, `商品主图=0`.
- Reused the prepared handoff `.temp/asi_has_inquiry_manifest/handoff_LP1558_20260612_2136.md`.
- Spawned exactly one LP1558 worker under the serial rule: session `agent:global-trading-product:subagent:aa2bf349-5fb5-4714-8efe-484f0527a927`, run `cd5cdc67-74ee-4f6a-b9e6-9298405642be`.

Lesson / next rule: after a completed product is recorded, live-check the next candidate immediately before spawning, because stale manifest candidates can already have main images.

## ASI queue dispatch failure - 22:52 user correction

Trigger: user challenged the claim that the queue was merely silent, saying a subagent could not have been running for over an hour with no response.

Evidence:
- LP1538 was completed, verified, delivered, and cleared at 21:35.
- LP1558 handoff was prepared, but the main session only yielded a status saying it was about to dispatch LP1558; the actual `sessions_spawn` did not complete then.
- Progress showed no running worker until the 22:51 heartbeat spawned LP1558.
- Therefore the 21:35-22:51 gap was not Oracle work and not subagent work; it was a parent orchestration gap.

Lesson / next rule: never treat “about to dispatch” or `sessions_yield` as successful dispatch. A product is active only after `sessions_spawn` returns a child session/run ID and `.temp/asi_has_inquiry_manifest/progress.json` has `running_count=1` for that worker. If a turn is interrupted during dispatch, the next check must verify whether spawn actually happened before saying the queue is running.

Follow-up correction:
- User asked whether `HEARTBEAT.md` was unclear or whether the assistant relied on historical messages.
- Root cause was not missing heartbeat wording about checking; the file already said to check subagents/queue and spawn the next worker.
- The failure was lack of a hard dispatch-completion check: the assistant treated prepared handoff / yielded “dispatching” status as progress without verifying `sessions_spawn` returned child IDs and `progress.json` showed `running_count=1`.
- `HEARTBEAT.md` was updated to require live checks of subagents, Oracle lock/processes, and `progress.json`, and to define spawn success only after both child session/run ID and progress writeback are present.
- User further corrected that heartbeat instructions should name concrete tools/actions, not just abstract “check” wording. `HEARTBEAT.md` was revised to require using subagents/sessions tools for worker status, shell/jq for `progress.json` and files, Oracle lock/process checks, current-time comparison with `started_at` / `last_parent_check`, live Base checks before dispatch, and `sessions_spawn` + progress writeback as the only proof of successful dispatch.

## ASI image queue heartbeat - 23:23

Context: heartbeat checked LP1558 after the corrected tool-level HEARTBEAT.md instructions.

Actions and outcome:
- Used live files, progress, session status, Oracle lock/process checks, and current time.
- LP1558 remained the only running product worker; session `agent:global-trading-product:subagent:aa2bf349-5fb5-4714-8efe-484f0527a927` was still marked running.
- Oracle lock was active for hero generation at the start of the check, then released; `hero_main_chatgpt_current_LOGO-BK.png` appeared.
- Local outputs now include clean reference, white background, lifestyle, selling point, dimension, material, SKU, and hero candidates.
- No `upload_main_*` or final readback evidence existed yet, so parent did not deliver or dispatch the next product.
- Parent sent a targeted nudge to the same LP1558 subagent to upload accepted final main images, save readback evidence, and report completion.

Lesson / next rule: if outputs are present but upload/readback evidence is missing, keep the same product active and nudge that worker; do not spawn recovery or the next product until upload/readback or a clear blocker is present.

## ASI image queue heartbeat - 2026-06-17 06:51

Context: heartbeat found `progress.json` still marked LP5500 as running, so the parent verified current state from tools and files instead of relying on history.

Actions and outcome:
- Confirmed no active ASI subagents; LP5500 subagent `agent:global-trading-product:subagent:c7cdca28-8f2f-45df-ab6d-2cd7b56e70db` / run `226902c4-32ab-408a-8616-66da4e1d2b84` was `done`.
- Confirmed no active LP5500/Oracle image-generation process.
- Verified LP5500 final worker report and Base readback counts: `参考图片=1`, `白底图=1`, `商品主图=6`.
- Accepted finals: `LP5500_clean_no_logo.png`, `LP5500_white_background_v2.png`, `LP5500_hero_main.png`, `LP5500_lifestyle_scene_v2.png`, `LP5500_selling_point.png`, `LP5500_dimension_chart.png`, `LP5500_material_detail.png`, `LP5500_sku_color_chart.png`.
- Rejected earlier `LP5500_white_background.png` and `LP5500_lifestyle_scene.png` because both had faint Gemini sparkle/AI marks.
- Since delivery had already been sent in Feishu, parent cleared the stale running entry without duplicating media delivery and did not spawn the next product in the same heartbeat.

Lesson / next rule: when final files and delivery have already happened but progress is stale, clear `progress.json` only after confirming subagent status, process absence, local outputs, and final Base readback; avoid duplicate user delivery.

Follow-up dispatch:
- After LP5500 was cleared, parent live-scanned the next manifest records.
- LP2104 was skipped because live Base already had `白底图=1` and `商品主图=2`.
- LP2105 (`recuYsAqeiuC4k`) was selected because live Base showed `有询单`, `参考图片=1`, `白底图=1`, and `商品主图=0`.
- Parent wrote `.temp/asi_has_inquiry_manifest/handoff_LP2105_20260617_0651.md`, spawned one worker only: session `agent:global-trading-product:subagent:76c79dd7-de40-4c88-9b01-ddf35c1b0660`, run `79846f72-5d4d-44cd-a573-43c7d6e5ad44`, and verified `progress.json` has `running_count=1`.

## ASI image queue heartbeat - 2026-06-17 09:27

Context: heartbeat found `progress.json` still marked LP2105 as running after LP2105 final delivery.

Actions and outcome:
- Confirmed no active ASI subagents; LP2105 original worker and color-revision worker were both `done`.
- Confirmed no active LP2105/ASI image-generation process from parent-side process inspection.
- Verified LP2105 revision readback: `参考图片=1`, `白底图=1`, `商品主图=7`.
- Final accepted LP2105 images included the revised SKU chart and revised logo hero; the old color-inconsistent SKU and hero uploads had been removed.
- Since LP2105 media had already been delivered in Feishu, parent cleared the stale running entry without duplicate delivery.
- Live-checked next manifest candidate LP2120 (`recuYsAqeipCZ6`): `参考图片=1`, `白底图=1`, `商品主图=1`.
- Wrote `.temp/asi_has_inquiry_manifest/handoff_LP2120_20260617_0927.md`, spawned exactly one LP2120 worker: session `agent:global-trading-product:subagent:17337450-87b8-4891-830e-dedabb8f3734`, run `0c924bd5-762d-450b-8b36-61898df8ae8c`, and verified `progress.json` has `running_count=1`.

Lesson / next rule: partially completed records with an existing hero can still be eligible; live-check counts and hand off a completion task that reuses usable existing images and avoids duplicate uploads.
