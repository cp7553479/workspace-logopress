<<<<<<< HEAD

## 2026-05-31 ASI 50-product image batch
- User requested selecting 50 bag/packaging/pouch/box/storage-type products from `LogoPress 外贸ERP` Base `SF9ibzjI4a0YFJsDZtKcLvtbnfh`, table `商品表` `tbl3KNB7zP8EQ1d0`, where `商品主图` was empty, checking `选品`, then generating ASI image sets and sending finished images to the user.
- Generated candidate view `AI-空主图包袋盒候选-20260530` (`vewbwDMpvO`) filtered empty `商品主图`; final selected list is `.temp/asi_50_candidates/selected_50.json`; progress tracking is `.temp/asi_50_candidates/progress.json`.
- Key field IDs: `产品编号` `fldHb6xDKX`; `白底图` `fldzPbQJ2z`; `商品主图` `fldn5dP24T`; `选品` `fldW9tz1Se`; `参考图片` `fldr9B4JZT`.
- Attachment operations for this task should use target Base `SF9ibzjI4a0YFJsDZtKcLvtbnfh` and table `tbl3KNB7zP8EQ1d0`; avoid the mistaken `KcXMbMUvAa7TNYsIqD7cwNi1nDf` / `tblsVUTaYTcvp8F5` values that appeared in early subagent prompts.
- User clarified: “Codex 生图” means the `generate-image` skill Codex script, specifically `node ~/.openclaw/skills/generate-image/scripts/codex-imagen.mjs`, not `image-cli`. Future Codex-lane subagents must read `~/.openclaw/skills/generate-image/SKILL.md`, run `--smoke --json`, and use `codex-imagen.mjs` with `--input-ref`/`--prompt-file`.
- Oracle image generation must remain serial; check active Oracle processes before starting a new Oracle generation.
- Completed, uploaded to the correct 商品表, and sent to user: LP0500 (`recuR6ZFNfAaQy`), LP6258 (`recuYsAdUpPWdV`), LP5863 (`recuYsAdUpYq3C`), LP1011 (`recuYsAdUpXFYO`), LP6444 (`recuYsAdUpFEmH`). Each has 1 `白底图` and 6 `商品主图` images confirmed after upload.
- LP1533 (`recuYsAdUpfw0G`) failed in the initial wrong Codex/image-cli lane due provider/auth/quota errors and produced no images; should be retried via the corrected Codex script or Oracle queue.
- At last known state, next batch was already running: LP0934 (`recuYsAdUp3fK7`) in Codex script lane and LP0956 (`recuYsAdUp0Wu6`) in Oracle lane. Next queued examples include LP0957 and LP0975.
- Generated product temp directories follow `temp/<产品编号>_asi_<record_id>/` with `references/`, `outputs/`, and `uploads/`; summaries are in `uploads/heartbeat-summary.md` when available.
=======
# 2026-05-31

## 飞书主 Base 切换确认

- Context: 文森在飞书私聊中说明，之前多维表格的 base token 全部换成 `https://mingjiainno.feishu.cn/base/SF9ibzjI4a0YFJsDZtKcLvtbnfh?table=tbl3KNB7zP8EQ1d0&view=vewMQ68Gd5`，table id 也以这个 Base 里的为准。
- Action: 按 lark-base 规则读取 `lark-cli --help`、`lark-cli base --help` 和 `+table-list` reference 后，执行 `lark-cli base +table-list --base-token SF9ibzjI4a0YFJsDZtKcLvtbnfh --limit 100`。
- Result: 当前 Base 可见 30 张表，关键表包括 询单 `tbluf7HTaj65eIAc`、PI `tblIu2Fc9CseBBtk`、客户联系人 `tblxmh9hT5UQMwMZ`、商品表 `tbl3KNB7zP8EQ1d0`、ASI商品表 `tbl1f731h82T86ag`、PI明细表 `tbl8WteY3lqMhI78`。
- Lesson: 以后飞书多维表格操作默认使用该 Base URL/token，并通过实时 `+table-list` 校验表 ID；历史旧 token / 旧 table id 不作为当前工作准绳。
>>>>>>> 39da275bed94029b53d500e75171514b13dd9185
