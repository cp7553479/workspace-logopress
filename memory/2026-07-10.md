# 2026-07-10 Memory

- 2026-07-10 11:17 Asia/Shanghai - ASI 商品图队列当前恢复状态：用户要求从主 Base「商品表」筛选 `商品主图` 为空且 `参考图片` 有图片的记录，按队列生成 ASI 电商套图；每个 subagent 只处理一个商品、不派生新商品图 subagent，最多两个商品并行。已完成并父级复核/飞书逐张发送/回写 Base 的商品包括 LP0538、LP0472、LP0539。LP0538：参考图替换为 clean no-logo，白底图 1 张，商品主图 5 张。LP0472：参考图 clean no-logo，白底图 accepted retry，商品主图 5 张；selling point 因英文问题未采用且不再追加，因为商品主图已非空。LP0539：参考图 clean no-logo，白底图 1 张，商品主图 6 张，8 张图已发送。
- 2026-07-10 11:17 Asia/Shanghai - ASI 商品图队列当前并行任务：LP0540 与 LP0542 正在处理。任一完成后 main agent 必须先父级复核：确认 subagent 开工前已回读该记录且 `商品主图` 为空，确认 Base 回读字段数量、图片上传、消息发送、1:1 与合规；然后继续补派下一个商品以保持最多两个并行。
- 2026-07-10 11:17 Asia/Shanghai - 用户新增硬规则：每个 subagent 开始处理商品前必须先检查该商品 `商品主图` 字段是否已有图片；若已有图片，立即跳过处理、结束 subagent 任务并汇报给 main agent；main agent 收到跳过汇报后继续派遣下一个商品任务。该规则已写入当前队列跟踪文件、handoff 和 HEARTBEAT。
- 2026-07-10 11:17 Asia/Shanghai - 用户强调每次 subagent 完成后 main session 必须完整复述任务详情和要求，像念经一样持续、重复地念出来，不能摘要、不能压缩、省略关键规则，并且要强调下次 subagent 完成后还要完整复述。当前完整任务要求包括：筛选主 Base 商品表中 `商品主图` 为空且 `参考图片` 有图片；每个 subagent 一个产品；最多两个并行；先清除参考图 logo 且保留其他部分不变；只移除 logo/sample logo/品牌图案/示例印刷/平台厂家标记；Oracle ChatGPT/Gemini 生图，ChatGPT 优先，Gemini 只负责无 Logo 和白底图，Gemini 不够用 ChatGPT，Oracle 都不够用 Codex 兜底；生成进度、任务详情、任务要求写入跟踪文件并让 subagent 阅读；生成图上传飞书多维表格并在 Feishu message 逐张发给用户；开工前检查 `商品主图`，已有则跳过并汇报；每次 subagent 完成后必须完整复述并提醒下次继续完整复述。

- 2026-07-10 16:13 Asia/Shanghai - Pre-compaction memory flush: no new task execution details were provided beyond the existing 2026-07-10 ASI 商品图队列 memory. Preserve the active rules already recorded today: use canonical `memory/2026-07-10.md` for flushes, never timestamped variants; do not edit bootstrap/reference files during memory flush; ASI 商品图队列 continues with max two products in parallel, per-product subagents only, each subagent must check `商品主图` before starting and skip if non-empty, and main session must parent-review, upload/verify Base fields, send images in Feishu, then fully repeat the task details/rules after each subagent completion.
- 2026-07-10 16:13 Asia/Shanghai - 用户更新 ASI 商品图队列复述话术：后续每次完整复述任务详情时，不要再说“若 Oracle 额度不够 / Oracle 都不够用，则使用 Codex 生图兜底”这句话；其他任务详情和要求继续完整重复。该更新是用户可见复述话术要求，不要求删除执行侧的失败留证/可用路径记录。
- 2026-07-10 18:08 Asia/Shanghai - ASI 商品图队列心跳进展：LP0564 已完成父级复核并确认 Base 回读 `参考图片=1`、`白底图=1`、`商品主图=8`；10 张 generated images 已由子任务通过飞书发送，尺寸图因仅有 imprint size 跳过，SKU 因仅有 MEDIUM WHITE 单色跳过。当前无活动旧子任务；已实时确认 LP0565 / `recuR8Sp872uRv` 与 LP0566 / `recuR8SuSDuYHV` 均为 `参考图片` 有图且 `商品主图` 为空，并派发两个新 subagent：LP0565 `agent:logopress:subagent:7efb722a-29ba-45d0-9334-58d4b6c96579`，LP0566 `agent:logopress:subagent:4df10a67-a3c4-4e79-9825-9ec247627eb6`。`HEARTBEAT.md` 和 `.temp/asi-image-task/status.md` 已同步。
