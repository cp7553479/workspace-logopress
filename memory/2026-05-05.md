- Worked on `skills/add-product` for the Feishu product lead Base; user requires live schema reads via `lark-cli base +field-list`, confirmation table before any create/update, record lookup by 产品编号, and no guessing for core numeric fields.
- Updated pricing logic for LP7304: `P1` must be computed by script as RMB cost -> shipping/fees -> divide by 7 to USD -> `ROUNDUP(..., 2)`. Wrote Q1~Q8 / P1~P8 to record `rec27gnhEPOPRC`.
- Important Base attachment finding: downloading a Base attachment by `file_token` can fail with HTTP 400 unless the request includes `extra.bitablePerm`. This was discovered by reading the raw Bitable record API response for LP7301 (`recvcuTH4emWUr`), whose attachment payload exposed `url`/`tmp_url` containing `extra={"bitablePerm":{"tableId":"tblsVUTaYTcvp8F5","rev":61527}}`.
- Successful attachment download pattern for Base attachments: call `GET /open-apis/drive/v1/medias/{file_token}/download` (or batch tmp download URL) as user and pass the `extra.bitablePerm` JSON in params.
- Downloaded LP7301 reference image to `/root/.openclaw/workspace-oss/tmp/lp7301-reference.jpg`.
- User explicitly wanted ACP Codex for image generation, but spawned Codex session reported no usable built-in image generation/editing tool exposed in that environment, so ACP Codex could not complete the white-background render there.
- Uploaded a white-background image file to LP7301 record `recvcuTH4emWUr` field `白底图` (`fldzPbQJ2z`) using `lark-cli base +record-upload-attachment`; returned file token `Skn4bDqt7o5bbpxxF3KczESCn1d`.
- User wants the `asi-product-image-generator` skill to keep the `generate-image` name as-is, keep `/assert/LOGO-BK.png` and `/assert/LOGO-WT.png` unchanged, and use description text that only states what/when (not how).
- For the ASI image workflow, plan files and downloaded reference images should live together under `/.temp/<custom folder name containing product number>/`, and the plan filename must include the product number.
- User prefers the first image label to be `首图` instead of `主图`.
- User wants one product handled at a time within the image-generation workflow; no batch generation for a single product, and next image should be generated only after the previous one is checked.
