# 2026-07-17

- Ongoing ASI product-image pipeline: keep exactly two active, unfinished product slots. When one closes its full loop, immediately query the LogoPress ERP 商品表 for the next eligible product (empty main image and a reference image; exclude completed/in-progress) and assign one dedicated worker.
- Each product requires generation, acceptance review, Feishu Base write-back, read-back/SHA verification, and individual image delivery. Never skip those stages; only one worker may write a product's attachments.
- Image fidelity is paramount: do not invent, alter, redraw, or omit real product structure, quantities, labels, or details. Logo-clearing may only remove the logo. A four-corner-star watermark at bottom-right is an acceptance signal but must never be put in a generation prompt.
- Current handoffs: LP0750 is blocked at clean-logo reference generation after Oracle browser connection failures; a Codex fallback was rejected because it damaged the real front-button pattern. Resume from this point only after the Oracle second-profile ChatGPT session is valid. LP0751 has an accepted, verified clean-logo image and should resume from its orange white-background image step after its first attempt failed with a local connection error and no output.
- Oracle/ChatGPT browser mode has recurring `ECONNREFUSED 127.0.0.1:<port>`, manual-login-session timeout, and no-output failures. Treat this as a browser-session availability issue, preserve validated work, and resume from the checkpoint after confirming the second profile is logged in.
- Operational rules and per-product checkpoints live in `HEARTBEAT.md`, `task-progress.md`, and each product's `.temp/LPxxxx_asi_<record_id>/` handoff files. HEARTBEAT includes a five-minute recovery audit covering worker state, Base state, cooling/retry timing, and exclusive write ownership.

## 2026-07-17 11:54 CST — corrected pipeline checkpoint

- The older entries above are superseded where they conflict with this checkpoint: the Oracle `second` browser profile was deleted. All future Oracle work must use only the `default` profile; do not recreate or use `second`.
- Fully closed and slot-released products: LP0747 and LP0749.
- LP0750 remains an active slot, blocked at the clean-logo-reference step. Oracle `default` did not detect a valid ChatGPT login session; Gemini subsequently terminated when its WebSocket closed. No output was generated, uploaded, or written to Base in this retry. Its handoff is `.temp/LP0750_asi_recuRcKFugmc2D/handoff.md`; resume only after a valid Oracle default ChatGPT session is available.
- LP0751 remains an active slot only because delivery is outstanding. All six images—including clean-logo, size, detail, two-color SKU, and final Hero—were generated, accepted, written back, read back, and SHA-256 verified. The final Hero used Codex fallback, Base read-back revision `8788`; evidence includes `uploads/hero_readback_1145.json` and `uploads/hero_reverse_download_1145.json`. The only remaining action is to send every accepted image individually in Feishu, but the recipient and sending identity have not been confirmed.
- Therefore, do not dispatch LP0752 or later yet: LP0750 and LP0751 still occupy the two unfinished-product slots. Continue the five-minute audit and recover stalled work without duplicate workers or concurrent attachment writes.

## 2026-07-17 13:13 CST — current pipeline checkpoint

- This checkpoint supersedes conflicting earlier entries. Fully closed and slot-released products are LP0747, LP0749, LP0751, and LP0750. LP0750’s last two assets—the two-color SKU image and final Hero—were accepted, written to Base, read back, reverse-SHA verified, and delivered; its evidence is in `.temp/LP0750_asi_recuRcKFugmc2D/handoff.md`.
- LP0752 is the sole currently occupied slot. Its clean reference, three color white-background images, scene image, and selling-point image were accepted, written to Base, read back, SHA verified, and delivered. After the selling-point completion, an unknown concurrent attachment write was detected. Preserve both distinct valid selling-point assets; do not delete either. Pause all later LP0752 steps (including uploading its locally prepared size-image candidate) until the attachment-write conflict is resolved and exclusive ownership is confirmed.
- With only LP0752 active, one processing slot is available. The next eligible candidate begins at LP0753, subject to the standard Base eligibility check and exclusive-worker assignment. Maintain the five-minute audit and immediately refill eligible vacant slots without exceeding two unfinished products.
- Keep using only Oracle `default` (the `second` profile is retired). Browser session failures require checkpointed recovery, never re-generation of already validated assets. Each product's handoff remains the authoritative resume record.

## 2026-07-17 20:13 CST — latest ASI pipeline and delivery checkpoint

- Supersedes earlier pipeline status where inconsistent. The active global objective remains: continuously process LogoPress ERP `商品表` entries where 商品主图 is empty and 参考图片 is present; one worker per product, at most two concurrent products, complete the ASI asset set, validate, write back and read back, then deliver accepted images to Vincent. Continue until the eligibility query is empty.
- Completed and verified in this later run: LP0760 and LP0761 full ASI sets; LP0763 final hero repair (hero now displays complete `LogoPress` and `YOUR LOGO`, with white/black SKUs); LP0764 hero and lifestyle; LP0766 clean reference, white/black white-background images, lifestyle, and white/black SKU images. LP0766 still needs its dimensions image and final compliant hero.
- LP0762 detected a concurrent worker on the same product and stopped before attachment writes; do not resume/upload without confirming exclusive ownership. LP0784 remains blocked on final hero generation after provider quota/timeout failures; its clean reference and white-background assets are already correct, and its lifestyle exists locally.
- The prior two long-running Oracle image-generation sessions that blocked work were no longer running when checked. Treat Oracle generation/browser failures as recoverable checkpoint issues; preserve accepted assets and resume only missing steps.
- Image delivery was previously missing because the OpenClaw app-server image-message handler was unregistered. The native Feishu IM image path now works: four accepted images were successfully sent (LP0763 hero and lifestyle; LP0766 lifestyle and white/black SKU). Use this native path for all subsequent accepted images, and backfill earlier completed assets as appropriate.
- Do not rely solely on chat-summary memory for exact attachment state. Re-read the relevant Base record and product `.temp/LPxxxx_asi_<record_id>/` evidence before retrying, uploading, deleting, or declaring completion.

## 2026-07-17 22:22 CST — ASI pipeline continuation checkpoint

- The active objective is unchanged: loop through LogoPress ERP `商品表` records with an empty 商品主图 and a non-empty 参考图片, process one product per worker with at most two workers simultaneously, validate every image, write and read back from Base, and deliver accepted assets to Vincent. The full operating rules are in `HEARTBEAT.md`.
- Confirmed fully complete products in this run are LP0754, LP0755, LP0759, LP0760, LP0761, LP0763, and LP0764. Never treat old duplicated completion notices as new work.
- LP0766 has accepted/re-read clean reference, white-background, lifestyle, and white/black SKU assets. Only the dimensions asset and compliant final hero remain.
- LP0767 recovered from a stalled lifestyle-generation session; its clean reference and white-background work are verified, dimensions are intentionally skipped because no reliable measurements exist, and a Gemini lifestyle retry was running at the checkpoint.
- LP0768 is paused for a real reference conflict: every source shows a double-plate clip, while the requested result describes a three-barrel curling iron. Clean reference and purple/pink white-background assets were written and verified, but do not generate further assets until the user either authorizes the double-plate form or supplies an authoritative three-barrel reference.
- LP0784 retains a valid lifestyle image and correct clean-reference/white-background fields, but its final hero was still in a ChatGPT retry after prior quota/timeouts and a Gemini attempt that distorted product structure. Do not upload a hero unless it preserves structure and clearly shows the required `LogoPress` text.
- LP0769 passed initial eligibility/material checks and its clean-no-logo Gemini generation session was running. LP0770 has verified clean reference and white-background upload; reject its first lifestyle candidate because it changed three red indicators to green and omitted/cropped the cable. A focused retry is required before any main-image write.
- LP0762 previously halted because another worker was acting on the same record; do not resume or attach local output until exclusive write ownership is re-established.
- Image delivery has an unresolved permission caveat: native Feishu IM delivery worked in some earlier cases, but current sending lacks `im:resource:upload` / `im:resource` and may not yield a delivery ID. Preserve files and Base write-back evidence; do not claim successful delivery without confirmation.
- Per-product evidence and outputs are in `.temp/LPxxxx_asi_<record_id>/`. Before action, re-read both Base state and those handoff/evidence files; preserve accepted attachments and never delete a concurrent worker's distinct output.

## 2026-07-17 22:22 CST — Oracle image-generation preference

- Vincent requires Oracle-initiated ChatGPT image generation to use Medium thinking strength; if a model-tier choice is used instead, select the equivalent Medium tier. This is recorded in `HEARTBEAT.md` for all future ASI image tasks.
