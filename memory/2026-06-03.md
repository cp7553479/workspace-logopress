# 2026-06-03

## LP0396 首图补全

- Context: 文森询问 LP0396 首图是否完成，随后要求生成首图。
- Action: 查 `商品表` 记录 `recuQyVKVH7dvJ`，确认 `商品主图` 仅有 scene、selling-point、material-detail、sku 四张，缺首图；下载 `白底图` `LP0396-white-background.png`，按既定规则只使用当前白底图与 `LOGO-BK.png`，未混入普通参考图、场景图、SKU 图或历史生成图。
- Result: 生成并上传 `LP0396-hero.png` 到 `商品主图`，上传后验表确认字段内已有 5 张图，新增 file_token 为 `DFLMbvU75opMp0xCcNpci0Q9n4e`。
- Lesson: 浅色/半透明产品首图优先使用黑色 Logo 文件；若只是首图补全，先验当前 `商品主图` 附件名，避免重复生成。

## LP7305 遮阳帽记录补充

- Context: 文森在飞书群聊中为刚新增商品记录补充参考图、货号、1688链接、1688价格和基础文字信息；记录 ID 为 `recvlmoRAxPQWi`，主商品表为 `SF9ibzjI4a0YFJsDZtKcLvtbnfh` / `tbl3KNB7zP8EQ1d0`。
- Action: 先为新记录上传 7 张参考图，后将 `/root/.openclaw/media/inbound/90918dbd-6957-4c6f-9fd1-da5051289057.jpg` 追加为第 8 张参考图。`lark-cli base +record-upload-attachment` 不接受绝对路径，需 `cd` 到文件目录后用相对文件名上传。
- Correction: 曾按当前最大号自动写入 `LP7306`，文森更正该商品产品编号应为 `LP7305`；已将新记录改为 `LP7305`。注意表中存在两条 `LP7305`，后续操作需用 record_id `recvlmoRAxPQWi` 锁定遮阳帽记录，避免误改旧的 rhinestone cap 记录。
- Fields updated: `来源(1688)` 写入 `https://detail.1688.com/offer/1032757086230.html?offerId=1032757086230&hotSaleSkuId=6212436822903&spm=a260k.home2025.recommendpart.44`；`1688价(元)` 写入 `16`。
- Product facts: LP7305 是遮阳帽；箱规 `60 x 50 x 40 cm`，`300个/箱`，`30kg/箱`。已补产品标题/名称、Description、完整描述、产品关键词、主题、分类、产品材质、产品颜色、产品类型、印刷方式等基础文字信息，并计算写入 `Q1 = 150`、`P1 = 3.78`。
