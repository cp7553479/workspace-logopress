## 12:13 CST — ASI product-image queue lessons and state

- The ASI workflow remains safety-critical around Feishu attachments: read and baseline the exact record before any write; treat `参考图片`, `白底图`, and `商品主图` as protected fields; stop rather than overwrite if an unowned/concurrent change appears. After upload, reread the record and verify the expected attachment set before releasing its lock or telling Vincent it is complete.
- Hero/main-image acceptance rule: branding must be plain `YOUR LOGO` text only. Reject/remove/recreate any render containing an LP/LogoPress graphic mark, an unexpected logo, watermark, or altered product details. Also reject control-panel/display images if readable digits or labels drift from the source (a prior LP1900 render changed `30` to `80`).
- LP2744 (`recuYsA78z3BLm`) is complete and delivered: one clean six-colour no-logo reference (black, blue, green, orange, purple, pink), six matching white-background images, and three main images (lifestyle, SKU chart, and plain-text `YOUR LOGO` hero) were reread in Feishu. Do not reprocess it unless its protected fields change.
- Image-generator availability is intermittent. Preserve prompts and failure evidence under each product's `.temp/<product>_asi_<record>/uploads/generation-evidence.md`; Oracle CLI v0.16.0 rejects `--profile`, and Oracle Gemini may fail with browser `fetch failed`. Codex image generation has been the successful fallback. Avoid blind repeated retries when no output file is produced.
- Vincent's document-conversion preference: for ordinary XLSX-to-PDF work, use direct LibreOffice headless export and validate the resulting PDF/page preservation. Do not route through Numbers or screenshot/manual reconstruction unless explicitly requested.
- Quotation skill dependency audit: its core runtime is Python with `openpyxl==3.1.5`; `pathlib` and `shutil` are standard-library modules. It also needs installed and authenticated `lark-cli --profile logopress` (observed v1.0.73). Use `skills/quotation/requirements.txt` for Python dependencies; `package.json` is optional only as a Node command wrapper and must not declare Python packages. No dependency files were changed; Vincent was asked to confirm the proposed files.
- Quotation skill template: the source workbook is `skills/quotation/templates/询价模版.xlsx`. The workflow copies this template and writes completed quote workbooks into the workspace `quotation/` output directory.
- 12:10 CST — Vincent authorized the quotation-skill dependency declaration. Added `skills/quotation/requirements.txt` pinning `openpyxl==3.1.5`, installed it at user level, and verified version 3.1.5. `lark-cli` remains a separately installed/authenticated external prerequisite.
## Quotation skill updates (12:40 CST)

- The quotation template is `skills/quotation/templates/询价模版.xlsx`.
- Added `skills/quotation/requirements.txt` with `openpyxl==3.1.5`; it was installed and verified at that version. `lark-cli` remains a separately installed external dependency.
- Added the small-parcel freight rule to `skills/quotation/SKILL.md`: when `报价表!B5 < 15kg`, use the greatest A-column tier not exceeding the computed weight. Read `上海UPS小货!B` into `报价表!B13`, and `空派小货（普货）!B` into `报价表!C13`. Example: 4.8kg matches 4.5kg (UPS 144; air 107). Below the smallest tier, stop for confirmation rather than round up. Because openpyxl does not calculate formulas, compute the B5-equivalent weight before lookup instead of trusting a cached formula value.

## Quotation skill clarification (12:49 CST)

- Vincent explicitly requires the small-parcel lookup to be driven by reading the current quote workbook at run time, not by a script with hardcoded tiers, prices, coordinates, or a duplicated B5 formula. Inspect the actual `上海UPS小货` and `空派小货（普货）` tables and their headers, read the actual evaluated B5 weight, floor-match the A-column tier, write the corresponding currently read prices to the actual `报价表` freight-price cells, then read back to verify. The prior wording about computing a B5-equivalent value should not be interpreted as permission to reimplement that formula in code.

## Quotation audit and ASI checkpoint (12:57 CST)

- Critical correction pending in `skills/quotation/SKILL.md`: template `报价表!B5` is an amount formula (`B17*B4`), not a billable weight. The first billable weight is `B19`; multi-quantity inputs/weights span `B4:F4` and `B19:F19`. Do not enable small-parcel tier matching against B5; revise the workflow to read the real billable-weight cell(s) from the current workbook.
- Small-parcel matching must remain workbook-driven: inspect current `上海UPS小货` and `空派小货（普货）` tables at runtime, use their numeric A-column tiers and current B-column prices, floor-match after selecting the correct actual billable weight, write and reread the freight cells. The previously observed 4.8 kg -> 4.5 kg, UPS 144 / air 107 result is only a structural validation, never a hardcoded rate. The UPS sheet header may misleadingly label its numeric tier column as “国家”.
- Before finalizing the quote workflow, require an explicit policy for the `<15 kg` / `>=15 kg` boundary, multi-quantity freight treatment, under-minimum/no-tier cases, duplicate/missing/non-numeric tiers, whether rates include surcharges, default FX/profit values, and public-edit permission. On any unhandled ambiguity, stop and ask rather than retain an old template value. Also correct the skill's `B4:F5` typo to `B4:F4`.
- Excel-to-PDF delivery should use a real scriptable converter (for example LibreOffice headless), not Numbers screenshots.
- LP4001 (`recuYsA78z3CVm`) completed for the required asset set: clean reference token `SU46bBwFQotYGhxe066cZR2Kndc`, white-background token `MbwFbGEgBoUudvx5IGIcm4yonna`, and text-only hero token `RmBCbthXko2FuPxGNAKcdpIBnNg`; Base readback confirmed exactly one attachment in each protected field. Evidence is under `.temp/LP4001_asi_recuYsA78z3CVm/evidence/`. Dimensions/SKU images were intentionally skipped because the source provides no reliable specifications and only one silver SKU.

## Quotation template correction (13:04 CST)

- The 12:57 CST quotation-audit entry is incorrect and superseded: `报价表!B5=$B$17*B4` is **total weight**, not money. `B17` is unit chargeable weight and `B4` is customer quantity. Small-parcel eligibility must therefore use `B5 < 15kg` as Vincent specified.
- Confirmed structure: `B4:F4` are customer demand quantities; `B5:F5` are the corresponding total weights; `B17:D17` contain unit chargeable weights for the transport methods; `B13/C13` are express/air-freight logistics unit prices. Do not substitute `B19` for this rule.
- Vincent requires full-workbook reasoning: before changing quotation logic, inspect the relevant sheet layout, headers, formula relationships, and helper sheets together—not isolated cells. The dynamic runtime-read/floor-match rule remains: read the live workbook's B5 weight and current tier tables, then verify the writeback; do not hardcode prices, tier rows, or duplicate workbook formulas in script.

## Quotation skill dependency and threshold finalization (13:06 CST)

- Added `skills/quotation/requirements.txt` with `openpyxl==3.1.5`; installed it user-wide and verified the installed version. The quotation template is `skills/quotation/templates/询价模版.xlsx`.
- Vincent explicitly finalized the shipment split: `报价表!B5 < 15kg` is small freight; `B5 >= 15kg` is large freight. The customer-quantity range is `B4:F4` (not `B4:F5`).
- For small freight, dynamically read the current workbook’s `上海UPS小货` and `空派小货（普货）` tier tables; floor-match the largest numeric A-column tier not exceeding B5, then write the live B-column rates to `报价表!B13` and `C13`, with source and destination readback validation.
