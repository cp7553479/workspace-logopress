# 2026-07-19 — ASI product-image queue checkpoint

- Batch objective remains: process only LogoPress ERP `商品表` records where product-main-image is empty and reference-image is present. Scheduler must lock one explicitly selected record (product number, record ID, record version, reference attachment token), never let a subtask choose/filter a SKU, and allow at most two different products concurrently. Re-read immediately before every remote write; any version/attachment mismatch pauses without overwrite. Keep these rules in `HEARTBEAT.md` while queue work continues.
- Fully completed and Base-backfill verified through the latest checkpoint: LP0754, LP0755, LP0759, LP0760, LP0761, LP0763, LP0764, LP0771, LP0774, LP0783, LP0785, LP0792, LP0795, LP0821, LP0823, LP0825, LP0833, LP0834, LP0835, LP0839, LP0842, LP0846, LP0848, LP0850, LP0851, LP0909, LP0916, LP0931, LP0939, LP0940, LP0941, LP0943, LP0944, LP0945, LP0946, LP0948, LP0974, LP2146. Most IM image-forwarding attempts remain blocked by Feishu resource-upload permissions/app message-processor registration; Base attachments are the authoritative completed output.
- LP0948 is fully complete: clean reference `KA1vbFf9SohPNKxmyPcchnnwnSh`, white background `AfzbbAq6PoRWGXxB7wecdUxXn3g`; verified main images: scene `QeMabdtM1otVjFxToIGc0xXhnig`, selling point `BeQVbHnuzo63NGxZUv5cKVacnEf`, size `Bu4Gb6lKCorxggxOEpzcKS1gnCd`, LogoPress hero/first image `HAISbKVh0oIxe9xpo0lccZvsn0e`. Its occupation lock was released; evidence is in `.temp/LP0948_asi_recuYsA4H0s1rl/STATUS.md`.
- LP0947: its existing `scene_codex.png` passed visual review and was uploaded/backfilled as main-image token `JdPQbkUDwoeUXux0X9Ucw4GUnLe`. Selling-point and final LogoPress-first-image generation are still in progress; retain its lock and use its `.temp/LP0947_asi_recu*/STATUS.md` evidence.
- LP0951 (record `recuYsA1RQaSlf`): clean reference and white-background backfill are complete. Two scene candidates were rejected (wrong product shape; unexplained mark). A Codex fallback scene generation is in progress; no main image uploaded yet. After an approved scene, complete selling point and final first image, with re-read protection. Status: `.temp/LP0951_asi_recuYsA1RQaSlf/STATUS.md`; lock remains active.
- Latest scheduler capacity: LP0947 and LP0951 are the active flows; one parallel slot was reported idle before this checkpoint. Before dispatching a replacement, perform the required read-only stability check and select/lock exactly one new eligible product. Do not retry or unlock products with unresolved conflicts/compliance failures without checking their current remote baseline.
- Operational history: legacy Oracle processes were terminated; heartbeat direct delivery was fixed (`session: main`, `target: last`, `directPolicy: allow`). Oracle ChatGPT invocation with unsupported `--profile` is known unreliable; use allowed fallback sequencing and visually reject malformed/fictional-product output rather than uploading it.

## Later durable updates

- LP2147 (`record_id=recuYsA1RQ9xQZ`) completed generation, visual verification, and Base backfill: one cleaned logo-free reference, ten color-variant white-background images, and four main images (SKU chart, size diagram, festival lifestyle image, and final hero image with LOGO-BK). Rejected candidates were not uploaded. Its occupation lock was released; delivery notification remains pending. Evidence/output directory: `.temp/LP2147_asi_recuYsA1RQ9xQZ/outputs/`.
- After LP2147 completed, review of the top 20 sorted candidates found historical conflicts or unresolved pending states for all of them; no immediately safe next candidate was identified. Continue only after a fresh remote-baseline/stability check, and never bypass per-product locking or conflict protection.
- Vincent prefers the AGENTS.md collaboration guidance to remain principle-based and concise, rather than listing detailed scenarios or rigid terminal-state mechanics. The agreed direction is one short paragraph: act as a communicative collaborator; clarify and confirm through multiple turns; treat cross-turn work as one continuous task and reuse confirmed results; when user input or judgment is required, ask directly and end that turn; tasks may be staged across turns; independent work may be delegated to spawned agents and the scheduler may wait for their results. This was a proposal only during this checkpoint; AGENTS.md was not edited during the memory flush.
- OpenClaw configuration was validated with `messages.visibleReplies: automatic`; `heartbeat_respond` is disabled globally through the top-level `tools.deny`. The scheduling rule was strengthened so the main loop remains active to receive subagent completion and dispatch the next safe candidate, instead of ending immediately after spawn.
- At Vincent's request, the global default and `logopress` Heartbeat prompt were changed to English: "Continue proactively completing the tasks in HEARTBEAT.md. If the tasks in HEARTBEAT.md no longer need attention, clear HEARTBEAT.md." Configuration validation passed and the gateway was restarted successfully.
- Vincent requested that all future formal subagent tasks use Claude. Claude may be slow to produce output, so do not impose a timeout; wait for its actual completion. The ACP default was confirmed as `claude`, and `HEARTBEAT.md` was updated to prescribe `sessions_spawn` with `runtime: "acp"` and `agentId: "claude"` for formal subtasks.

## Dispatch-method correction and current queue state (evening)

- The preceding Claude-only ACP instruction was superseded after a no-output greeting test and a 90-second wait-layer disconnect. Codex ACP also failed with `ACP_TURN_FAILED`. Formal ASI subtasks must therefore use OpenClaw `sessions_spawn` with `runtime: "subagent"` (not ACP/Claude) until the ACP runtime is repaired. This dispatch method and the scheduler safeguards were written into both `HEARTBEAT.md` and `AGENTS.md` on 2026-07-19.
- Permanent scheduling safeguards: the main scheduler alone filters/selects an exact SKU and records product number, record ID, record version and reference attachment token before dispatch; create one product lock; a subtask may process only that SKU and may not filter/select/replace/guess other records; no concurrent flow for the same SKU; at most two distinct products concurrently. On completion, scheduler re-reads and verifies, releases its lock, then immediately selects/locks a next safe candidate. The main scheduler, not a subtask, is responsible for any user-facing image delivery.
- Current active/incomplete work at the latest handoff: LP0947 still needs selling-point and final LogoPress hero/first image; LP0951 has clean reference and white background completed, while its approved scene/selling-point/final hero remain pending after rejected scene candidates; LP6406 has clean reference, 11 white-background color variants and hero backfilled, but needs scene and selling-point images; LP0910 and LP0913 are running under the native-subagent method (LP0913 has completed the cleaned-reference replacement and continues its remaining package). Respect their existing locks and verify cloud state before any follow-up.
- Heartbeat interval was shortened to 10 minutes while this queue is active, after a prior scheduler loop ended too early and left a slot idle. The English heartbeat prompt remains: "Continue proactively completing the tasks in HEARTBEAT.md. If the tasks in HEARTBEAT.md no longer need attention, clear HEARTBEAT.md."

## 21:05 CST compaction checkpoint

- LP0913 and LP0910 subsequently completed full generation, verified Base backfill, and image delivery to Vincent. The verified-complete count reached 44. LP0910 delivered 18 images; SKU color/size correspondence should still receive manual review before release.
- LP0943 and LP0944 were dispatched as the two recovery flows after LP0910. LP0943 previously reached only clean-reference and white-background stages; scene output changed the product shape and was rejected. LP0944's first candidate was rejected for a suspected lower-right watermark plus altered product-block structure/numbering; no rejected output was uploaded. LP0944 was re-dispatched specifically for one compliant, non-watermarked replacement.
- Current in-progress/recovery set also includes LP0947 (selling-point and final LogoPress-first image pending), LP0951 (scene fallback in progress, then selling-point and final first image), and LP6406 (scene and selling-point pending after clean reference, 11 white-background color images, and hero backfill). Verify Base uploads after every subtask report before user delivery; retain locks until the scheduler's verification is complete.
- Image-delivery rule is now explicit: workers report generated paths/tokens only; the main scheduler re-reads the Feishu Base to confirm attachment persistence and then sends the verified image(s) to Vincent. Workers must not message the user directly. Earlier delivery failures were largely permission/app-processor related; Base backfill is still the primary success condition.
- ACP remains unavailable for formal work: Claude produced no output before the 90-second wait-layer disconnect and Codex returned `ACP_TURN_FAILED`. Continue formal ASI work only through `sessions_spawn` with `runtime: "subagent"` until runtime repair. Existing locks and the two-at-a-time limit remain mandatory.
