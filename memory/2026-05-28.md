# 2026-05-28 Memory

## ASI image generation queue status - 02:06 Asia/Shanghai
- Active user goal: continue generating ASI ecommerce product image sets for selected Feishu 商品表 products using multi-agent workflow; max 2 subagents at a time; each subagent handles exactly 1 product; each subagent timeout 1 hour; Oracle and Codex image generation should alternate where possible; Oracle must remain serial and not concurrent.
- Base/table/view: Base token `KcXMbMUvAa7TNYsIqD7cwNi1nDf`; table `tblsVUTaYTcvp8F5`; selected view `vewS5HTB1k` / “已选中的产品”. Use current raw Bitable API pagination to re-verify queue before dispatch decisions.
- Core workflow rules still active: per product create/use `temp/<产品编号>_asi_<record_id>/` with `AGENTS.md`, `references/`, `outputs/`, `uploads/`; create/update `AGENTS.md` before image generation; download current product `参考图片` and `白底图`; if `白底图` is empty, generate and upload white background first; webSearch before prompts; prompt English only, 1:1, current product references required, no Chinese/prices/discounts/currency/QR/watermarks/manufacturer/supplier/platform/contact/URL/unsupported logos; do not invent SKU/material/structure/dimensions/features/accessories; final images must be produced through image generation capability, not script/PIL/ImageMagick/SVG/canvas compositing.
- Main/first image rule: generate last; must use official Logopress logo from skill assets only (`LOGO-BK.png` for light print areas, `LOGO-WT.png` for dark print areas); not pure white background; product dominant about 80%; right side three circular thumbnails about 20%; optional bottom SKU row only if multiple confirmed SKUs; no marketing copy/text.
- Completed and parent-verified during recent run: LP2675 (6 商品主图 + 1 白底图), LP2765 (6 商品主图 + 1 白底图), LP2767 (5 商品主图 + 1 白底图; SKU skipped because only one confirmed patriotic colorway), LP2591 (6 商品主图 + 1 白底图), LP2287, LP2401, LP2581, LP2443 after logo-fixed main image, and earlier completed products per prior memory.
- Current primary no-main queue after latest table scan before compaction: LP2737 `recuYsACgfrkSY`, LP3307 `recuYsACVI8dBp`, LP3413 `recuYsANVGbVrK`, LP3511 `recuYsANVGVUG6`, LP3755 `recuYsANVGoBhj`, LP4856 `recuYsAZk3jFZ6`, LP4911 `recuYsB9L5e3h0`, LP4913 `recuYsB9L5DRx7`. Latest scan showed LP3307 already had 白底图 1 but 商品主图 0; all others in this list had 白底图 0 and 参考图片 1.
- Partial queue to handle after primary no-main queue unless user changes priority: LP0124 (主图 4), LP0128 (4), LP0219 (3), LP1115 (3), LP1822 (4), LP1996 (4), LP7297 (4); all had 白底图 1 and 参考图片 1 in latest known scan.
- Active/just-spawned subagents at compaction time: `asi_images_lp3307_finish` was running as Codex/non-Oracle slot; `asi_images_lp2737_codex_rescue` was just spawned after Oracle retry failed. Do not poll immediately after spawn; wait for completion events. Keep max 2 concurrent subagents.
- LP2737 details: workspace `temp/LP2737_asi_recuYsACgfrkSY/` exists with `AGENTS.md`, `outputs/prompts.txt`, `outputs/manifest.json`, reference `4134231411759573293209.jpeg`, `LOGO-BK.png`, `LOGO-WT.png`, and upload/failure JSONs. Product is black insulated tote/lunch bag with confirmed dimensions and black/red/blue SKUs; use `LOGO-WT.png` on dark bag front. Multiple non-Oracle routes failed (OpenAI image CLI route no reference support, Qwen invalid API key, Seedream 401, MiniMax provider error, direct OpenAI placeholder key, Gemini monthly spend cap). Oracle retry failed because browser landed on ChatGPT A/B preference page and produced no image. Parent verified table still `参考图片=1`, `白底图=0`, `商品主图=0`. Current rescue task should prioritize generating/uploading white background via Codex/OpenClaw image route that accepts reference images, then continue full set.
- LP3307 status before compaction: `asi_images_lp3307_finish` was active/running. Latest table state before dispatch showed LP3307 `商品主图=0`, `白底图=1`, `参考图片=1`; task scope is to finish 商品主图 set using existing white background, with main image last and official logo.
- Dispatch plan after current active subagents finish: parent-verify local PNGs with `file` and raw Bitable fields before claiming completion. When a slot frees, continue primary queue in order, preserving max 2 concurrent and Oracle/Codex alternation where possible: LP3413, LP3511, LP3755, LP4856, LP4911, LP4913. If Oracle remains blocked by browser preference page, report blocker and continue with Codex-capable route rather than silently fabricating images.
- lark-cli update note: `lark-cli update --check --json` reported current version 1.0.29, latest 1.0.42, skills out of sync. User was told to update after image batch stabilizes; do not update during the critical image queue unless explicitly chosen.
- Feishu user communication: visible progress messages should be sent with `message(action=send)` because this is a Feishu direct conversation. Normal final replies are private only. User prefers concise, honest progress updates and dislikes claims like “I will keep running” when not actually guaranteed.

## ASI image delivery correction - 10:32 Asia/Shanghai
- Trigger: 用户提醒“生成好的图片你要发给我，记得及时UPDATE plan”。
- Lesson: 每个商品完成后不能只说已上传/已完成；父级复核通过后必须用 OpenClaw `message` 工具把该商品生成好的图片发给用户，再更新 plan 并派下一个商品。
- Next rule: ASI 单品流程固定为：subagent 完成事件 -> 父级验本地 PNG + 飞书字段 -> 通过 `message` 发生成图给用户 -> `update_plan` 记录完成/下一位 -> 再启动下一 subagent。
