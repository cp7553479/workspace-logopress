# 2026-07-15

## ASI dynamic product-image queue

- WG1736 is fully completed and uploaded to Feishu Base. Its approved cleaned reference token is `HJ5lbC4jUom82nxETSvcUjDpn4t`; white-background token is `VRuqbQFS2oD4BtxcwMhcxwhinlb`; four main-image tokens are `Lspdbi5P3o4pu8xBPAqcSLEUnIh`, `M24rbKsakonxbfxcRP0cHrWan5b`, `Jeg8bLRFbo85mCxjyfQcEpezneg`, and `EAzIbf23Aoii0ZxCuQAcwuf7nSd`.
- LP0737 was prepared as the one permitted replacement candidate while LP0735 remains open: Base record `recuRcJVJ1VSel`, reference attachment token `HnsobZFWDoBgvbxINw9cgrPin2d`. Its handoff is `.temp/asi-dynamic-queue-20260713/HANDOFF-LP0737.md`.
- LP0735 has an existing handoff at `.temp/asi-dynamic-queue-20260713/HANDOFF-LP0735.md` and local outputs at `.temp/LP0735_asi_recuRcH4RCsr1X/outputs/`, but has not been closed out or written back.
- Both LP0735’s finishing task and LP0737’s execution task are blocked by ACP executor failures (`AcpRuntimeError`, `ACP_TURN_FAILED`) that occur before productive work starts. Retries/configuration repair did not yield a stable session. No new images, uploads, or Base data were changed for either item by the failed tasks.
- Next action after the OpenClaw gateway/ACP service is restarted: re-dispatch LP0735 finalization and LP0737 execution; do not open more candidate products.
- Diagnosed configuration-level cause: the `acpx` and `codex` plugins are disabled or absent from the OpenClaw allowlist, preventing ACP sessions from starting. Recovery requires correcting that configuration and restarting the gateway; then resume only the two existing lanes.
- Queue execution constraints: one candidate at a time; ChatGPT is preferred for generation; Gemini is only for logo cleanup and white-background images; logo cleanup must remove only the logo; deliver/upload images one at a time; prompts must not mention four-corner stars; no extra subagents or multiple candidate queries.

## Later status update

- The plugin/gateway issue was manually bypassed by starting dedicated Codex ACP execution lanes for the two already-approved items only. LP0735's lane exited abnormally immediately, with no generation, upload, or Feishu Base write-back; its handoff and six existing local output files remain intact.
- LP0737's dedicated execution lane is still running. On completion, verify every upload and Base field write-back, then update the queue progress/heartbeat files. Do not query or start any further candidate products.
- LP0735 still needs a fresh, scoped finalization/retry after LP0737 is verified; preserve its existing outputs and do not redo unrelated queue work.
# 2026-07-15

## ASI dynamic product-image queue

- WG1736 is fully completed and uploaded to Feishu Base. Its approved cleaned reference token is `HJ5lbC4jUom82nxETSvcUjDpn4t`; white-background token is `VRuqbQFS2oD4BtxcwMhcxwhinlb`; four main-image tokens are `Lspdbi5P3o4pu8xBPAqcSLEUnIh`, `M24rbKsakonxbfxcRP0cHrWan5b`, `Jeg8bLRFbo85mCxjyfQcEpezneg`, and `EAzIbf23Aoii0ZxCuQAcwuf7nSd`.
- LP0737 was prepared as the one permitted replacement candidate while LP0735 remains open: Base record `recuRcJVJ1VSel`, reference attachment token `HnsobZFWDoBgvbxINw9cgrPin2d`. Its handoff is `.temp/asi-dynamic-queue-20260713/HANDOFF-LP0737.md`.
- LP0735 has an existing handoff at `.temp/asi-dynamic-queue-20260713/HANDOFF-LP0735.md` and local outputs at `.temp/LP0735_asi_recuRcH4RCsr1X/outputs/`, but has not been closed out or written back.
- Both LP0735's finishing task and LP0737's execution task are blocked by ACP executor failures (`AcpRuntimeError`, `ACP_TURN_FAILED`) that occur before productive work starts. Retries/configuration repair did not yield a stable session. No new images, uploads, or Base data were changed for either item by the failed tasks.
- Next action after the OpenClaw gateway/ACP service is restarted: re-dispatch LP0735 finalization and LP0737 execution; do not open more candidate products.
- Diagnosed configuration-level cause: the `acpx` and `codex` plugins are disabled or absent from the OpenClaw allowlist, preventing ACP sessions from starting. Recovery requires correcting that configuration and restarting the gateway; then resume only the two existing lanes.
- Queue execution constraints: one candidate at a time; ChatGPT is preferred for generation; Gemini is only for logo cleanup and white-background images; logo cleanup must remove only the logo; deliver/upload images one at a time; prompts must not mention four-corner stars; no extra subagents or multiple candidate queries.

## Later status update

- The plugin/gateway issue was manually bypassed by starting dedicated Codex ACP execution lanes for the two already-approved items only. LP0735's lane exited abnormally immediately, with no generation, upload, or Feishu Base write-back; its handoff and six existing local output files remain intact.
- LP0737's dedicated execution lane is still running. On completion, verify every upload and Base field write-back, then update the queue progress/heartbeat files. Do not query or start any further candidate products.
- LP0735 still needs a fresh, scoped finalization/retry after LP0737 is verified; preserve its existing outputs and do not redo unrelated queue work.

## LP0735 & LP0737 closure (09:00)

- LP0735 is fully closed: reference 1, white-bg 1, 5 product main images (hero, lifestyle, selling-point, sku-chart, dimension) uploaded to Feishu Base and verified. 7 images delivered to user via Feishu DM.
- LP0737 is fully closed: reference 1, white-bg 1, 5 product main images uploaded and verified in Base record `recuRcJVJ1VSel`. Original logo-bearing reference replaced. 7 images delivered to user via Feishu DM.
- After LP0735 and LP0737 both closed, the next step is to query Base for the next eligible record (商品主图 empty + 参考图片 has image) and continue the dynamic queue with a fresh subagent.
- HEARTBEAT.md was emptied after both lanes closed, but heartbeat polls continue; if the queue should continue, a new heartbeat entry and progress tracker should be written.

## Key lessons

- ACP runtime errors (`AcpRuntimeError`, `ACP_TURN_FAILED`) were caused by plugin allowlist misconfiguration; workaround was using Codex native subagent lanes via `sessions_spawn` with `runtime:acp, agentId:codex`.
- When a subagent's Base write-back fails partially, always read back the Base record to confirm actual field state before retrying; never assume fields are empty.
- The dynamic queue pattern (query one candidate at a time, spawn one subagent per product, max 2 parallel lanes) works well but requires careful handoff documents to avoid lost context across compaction boundaries.
- Gemini's four-corner star watermark in bottom-right is acceptable; this must NOT be included in generation prompts.
