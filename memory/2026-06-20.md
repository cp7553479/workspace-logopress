# 2026-06-20

- LP2994 ASI image audit: user correctly pointed out that I skipped the required clean-reference step. Evidence in `.temp/LP2994_asi_recuYsACVI4xsE/progress.json` recorded the original reference had a visible sample `YOUR LOGO` imprint, but `.temp/asi_has_inquiry_manifest/progress.json` showed `clean_reference_count=0`, no `references/cleaned/` file existed, and final Base readback still kept the original `参考图片` token `RX4KbDWBnosANpxC3kVcIOX9nWh`. Future ASI workers must distinguish white-background generation from clean reference generation: when the original `参考图片` has sample logos, marks, insets, or disallowed imprint content, first generate a clean no-logo reference and replace/upload it to `参考图片` before downstream white-background/main-image generation.

