# 2026-06-16 Memory

- ASI queue status before compaction (09:35 Asia/Shanghai): LP2058 was completed and cleared on 2026-06-15. Parent review passed, corrected dimension image replaced the old dimension image, and final Base readback was `参考图片=1`, `白底图=1`, `商品主图=5`. Seven LP2058 images were sent to the user: clean reference, white background, hero, lifestyle, selling point, corrected dimension, and material/detail. SKU image was skipped because reliable evidence supported only Clear as a single SKU.
- ASI queue monitor state: after LP2058 completion, `progress.json` showed `running_count=0`, `last_completed_product=LP2058`, no Oracle lock, no active ASI subagents, and queue counts retry/rework/blocked/deferred all 0. Completed queue count had reached 201 at one heartbeat check.
- Live selection refresh after LP2058: prior stored refresh at 2026-06-15 08:53 scanned 800 records and found no next eligible live record (`eligible_empty_main=0`). Later heartbeat attempted a fresh live scan; lark-cli `+record-list` returned JSON shaped as `data.data` array plus `fields`/`record_id_list`, not `.data.items`, so jq filters assuming `.items` failed and should be adjusted for the actual output shape.
- ASI queue operating rule remains important: keep product image work serial. Do not spawn a new product subagent if any ASI image worker is active or an Oracle/image-generation lock is present. Use the current Feishu direct-session cron monitor, not HEARTBEAT.md, for this long-running ASI queue.
- LP6409 ASI image task completed on 2026-06-16: parent verified final Base readback `参考图片=1`, `白底图=1`, `商品主图=6`, local PNG metadata, and key visual samples. Sent 8 images to the user: clean no-logo reference, white background, lifestyle, selling point, material/detail, dimensions, SKU chart, and hero main. Dimension chart uses Length 22.50 inch, Width 15.50 inch, Depth 1.50 inch. `progress.json` was cleared to `running_count=0`, `last_completed_product=LP6409`, completed count 202.
- LP2079 ASI image task completed on 2026-06-16: parent rejected the first hero because the right-side inset composition exceeded the limit / was not stable, then a same-product hero correction subagent regenerated the hero. Parent visually reviewed the corrected hero and live-read Base as `参考图片=1`, `白底图=1`, `商品主图=5`; old rejected hero token `UlhtbvJSaomsSIx4M5vch3TbncJ` was removed and corrected hero token `M61ib0miVou7zyxXxzdc6jH9n5g` remained. Sent 7 final media files: clean reference, white background, lifestyle, selling point, dimension, SKU chart, and corrected hero. `progress.json` was cleared to `running_count=0`, `last_completed_product=LP2079`.
- LP2085 ASI image task completed on 2026-06-16: subagent generated clean reference, white background, and scene, then stopped at selling-point generation block. Parent used the image generation skill to complete selling point, dimension, material/detail, SKU chart, and LOGO-WT hero; visually reviewed all outputs and live-read Base as `参考图片=1`, `白底图=1`, `商品主图=6`. Sent 8 final media files: clean reference, white background, scene, selling point, dimension, material/detail, SKU chart, and hero main. `progress.json` was cleared to `running_count=0`, `last_completed_product=LP2085`.
