# ASI product-image worker handoff: LP0029

Process exactly one product record from `商品表`.

- Product number: `LP0029`
- Record ID: `recuLkSwkhXWL7`
- Base token: `SF9ibzjI4a0YFJsDZtKcLvtbnfh`
- Table ID: `tbl3KNB7zP8EQ1d0`
- Parent livecheck: `.temp/asi_has_inquiry_manifest/live_record_LP0029_parent_20260626_120235.json`
- Workspace to use: `.temp/LP0029_asi_recuLkSwkhXWL7`

Mandatory routing and skill rules:

- You must read and use `.temp/asi_has_inquiry_manifest/worker_instructions.md`.
- You must read and use `/Users/vincent/.openclaw/workspace-logopress/skills/asi-product-image-generator/SKILL.md` as the governing ASI套图/image workflow.
- Because `白底图` is empty in the parent livecheck, also read and use `/Users/vincent/.openclaw/workspace-logopress/skills/generate-white-background/SKILL.md`.
- Read and use `/Users/vincent/.openclaw/skills/generate-image/SKILL.md` for image generation routing.

Current live status from parent:

- `参考图片`: 1
- `白底图`: 0
- `商品主图`: 5 existing images named `01.jpg`, `02.jpg`, `03.jpg`, `04.jpg`, `05.jpg`
- `Mark`: `有询单`

Task:

1. Create the product workspace with `references/`, `outputs/`, and `uploads/`.
2. Download only this product's current `参考图片`, `白底图` if any, and existing `商品主图` attachments needed for inspection.
3. Inspect whether the reference image contains logo artwork, sample logos, brand marks, imprints, manufacturer/platform marks, QR codes, watermarks, contact info, or URLs. If so, clean the reference according to `worker_instructions.md`, upload the cleaned reference to `参考图片`, remove the marked original, and read back.
4. Generate and upload a valid `白底图` because the field is empty.
5. Use the ASI skill rules to decide which final ASI套图 images are still needed. Existing `商品主图` count is 5, so complete the record to a reliable ASI image set without inventing unsupported details. Upload every newly accepted final image to `商品主图`.
6. Save prompts, upload responses, readbacks, accepted/rejected evidence, and a final report in the product workspace.
7. Final answer must include product number, record id, generated files, reference/readback status, uploaded fields, paths that the parent must send, skipped image types, and blockers if any.

Image-generation policy:

- Try Oracle first for every new image-generation attempt.
- Within Oracle, alternate ChatGPT and Gemini across images/attempts whenever possible.
- Use non-image-CLI Codex image generation only after Oracle ChatGPT and Oracle Gemini are both blocked or rejected with concrete evidence.
- Do not use the image CLI.
- Use English prompts only and avoid Chinese text, prices, QR codes, watermarks, supplier/manufacturer/platform/contact/URL information.
