# ASI Product Image Handoff - LP7244

Assigned product: LP7244
Record ID: recvcuSN91rpv1
Base token: SF9ibzjI4a0YFJsDZtKcLvtbnfh
Table ID: tbl3KNB7zP8EQ1d0
Table: 商品表

You must use the `asi-product-image-generator` skill as the governing ASI套图/image workflow. This is mandatory, not optional. Read and apply:

- `/Users/vincent/.openclaw/workspace-logopress/skills/asi-product-image-generator/SKILL.md`
- `.temp/asi_has_inquiry_manifest/worker_instructions.md`
- this handoff file

Scope:

- Process exactly one product record: LP7244 / `recvcuSN91rpv1`.
- Do not batch, inspect, upload, or modify other products.
- Create and use `.temp/LP7244_asi_recvcuSN91rpv1/` with `references/`, `outputs/`, `uploads/`, and `readbacks/`.

Live check at parent dispatch:

- `Mark`: 有询单
- `参考图片`: 1 image, `14558817551772257655335.jpeg`
- `白底图`: empty
- `商品主图`: empty
- Product name: `6 pcs nurse pen set with LED flashlight scissors markers`
- Description: `Celebrate Nurses Day with this all-in-one portable pen set, perfect for gifting and professional use. Rush service is available.`
- Product type: `Writing Instruments`
- Colors: `Black, pink, purple, blue, green, silver, orange, red`
- Material: `ABS`
- Size fields: length 18 cm, width 12 cm, height 0 cm
- Live-check file: `.temp/asi_has_inquiry_manifest/livecheck_LP7244_20260625_1621.json`

Required work:

1. Download only LP7244's current reference attachment.
2. If reference contains logo artwork, sample logos, brand marks, imprints, manufacturer/platform marks, QR codes, watermarks, contact info, or URLs, remove those visible marks with image generation while preserving the product shape, structure, color, material, proportions, angle, background, lighting, shadows, texture, and non-logo details. Upload the cleaned result back to `参考图片`, remove the original marked reference, and read back to confirm only the cleaned reference remains.
3. Because `白底图` is empty, generate and upload a valid white-background product image first.
4. Generate valid ASI套图 images according to the `asi-product-image-generator` skill. Skip image types that lack reliable support.
5. Upload every accepted final ASI image to `商品主图`.
6. Read back `参考图片`, `白底图`, and `商品主图` after uploads.
7. Write a final report with product number, record id, generated files, upload responses, final readback path/counts, rejected/skipped notes, blockers if any, and parent-deliverable image paths.

Route constraints:

- Try Oracle first for every new image-generation attempt.
- Within Oracle, alternate ChatGPT and Gemini across images/attempts whenever possible.
- Use non-image-CLI Codex image generation only after Oracle ChatGPT and Oracle Gemini are both blocked or rejected with concrete evidence.
- Do not use `image CLI`.
- Do not use scripts or programmatic compositing for final product images.
- Use English prompts only.
- No Chinese text, prices, QR codes, watermarks, supplier/manufacturer/platform/contact/URL information in images.
- Do not include product number, record ID, or internal IDs in image prompts or visible image text.
- Each generation must use at least one current product reference image.

Delivery:

- Do not send Feishu messages from the worker.
- Return final output paths so the parent can verify and deliver.
