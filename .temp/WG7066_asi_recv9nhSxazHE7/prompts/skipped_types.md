# Skipped Image Types

- 尺寸图: skipped. The record has numeric fields `产品尺寸 长(cm)=96`, `产品尺寸 宽(cm)=70`, and `产品尺寸 高(cm)=74`, but for an apparel compression shirt the dimension semantics are not clear enough to label a reliable product length/width/height diagram without risking incorrect measurement labels.
