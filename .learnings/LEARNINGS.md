
## 2026-05-12 - ASI product main image logo must not be script-composited
User corrected that product main images with logo must not be generated by local script compositing. For ASI product image generation, pass the logo image file as a reference to image generation; do not use PIL/ImageMagick/local scripts to composite logo onto product images. If a script-composited result was created, remove it and do not upload/use it.

## 2026-05-12 - ASI image generation should use ACP Codex task folders
- What happened: User corrected that ASI image workflow needs ACP Codex, product-specific temp folder, AGENTS.md, white-background-first flow, and strict logo references.
- Do differently: For ASI products, create `temp/<product>_asi_<record_id>/` with `AGENTS.md`, references, outputs, uploads; use `sessions_spawn(runtime="acp", agentId="codex", streamTo="parent", cwd=<product_temp_dir>)`; require output path verification and no script compositing.
